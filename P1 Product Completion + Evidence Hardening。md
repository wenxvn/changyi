你正在继续维护仓库：

`https://github.com/wenxvn/changyi`

项目：**常医智导**

当前最新基线请先核实，上一轮验收时为：

```text
a1932fba03aa7bd28d3e84bd4e512cd57e91441f
refactor(recommendation): rank by clinical fit, drop academic/legacy scores
```

当前项目已经完成：

* React + TypeScript 正式前端；
* Flask `/api/v1/*` 正式 API；
* Safety Gate；
* Structured Follow-up；
* 用户定位 / 区域位置 / unknown 三态；
* 医院与医生推荐；
* 医院/医生详情；
* OpenStreetMap 真实地图；
* 高德外部导航；
* Trust Center；
* 医院旧能力分退出正式排序；
* 医生科研指标基本退出 patient-fit 主排序；
* Playwright / CI / Safety regression。

因此：

# 不再进行架构重构。

本轮目标是：

# P1 Product Completion + Evidence Hardening

核心任务：

```text
1. 清理仓库遗留垃圾
2. 恢复医生照片
3. 恢复医院 Logo / 合法视觉资产
4. 恢复医疗资源高级筛选
5. 恢复简化专家偏好
6. 实施 Near-duplicate Group Split
7. 建立交通数据正式 Quality Gate
8. 恢复可信的交通可达性解释
9. 恢复简单、安全的语音输入
10. 扩展真实浏览器与移动端验收
```

不要把旧版全部搬回来。

---

# 1. 工作原则

必须遵守：

```text
正确性
>
医疗安全
>
数据可信度
>
产品闭环
>
可用性
>
视觉
>
功能数量
```

不要：

```text
继续拆 backend
换 Flask
换 React
接 LLM
接 Agent
做账号系统
做云病历
新增全国城市
恢复 Dashboard
恢复 Demo Login
恢复浮动 AI Assistant
恢复测试员后台
恢复自动轮播
```

---

# 2. 开始前读取

至少读取：

```text
README.md
AGENTS.md
memory.md

docs/status/current.md
docs/architecture/current-state.md
docs/risks/register.md
docs/quality/quality-gates.md
docs/POST_REFACTOR_BACKLOG.md
docs/REFACTOR_CHANGELOG.md

ui-registry.md
```

重点检查：

```text
frontend/src/
backend/app/
tests/
evaluation/model/
data_validation/
data/regions/320400/
data/doctors_*.json
static/images/doctors/
static/images/hospitals/
.github/workflows/quality.yml
```

运行：

```bash
git status --short --branch
git log --oneline -10
git diff
```

以真实仓库状态为准。

---

# 3. 第一件事：仓库卫生清理

当前根目录存在：

```text
1.md
```

它是 Agent Prompt，不属于正式项目。

确认无依赖：

```bash
git grep "1.md"
```

之后删除。

---

当前还存在：

```text
qa-screenshots/
```

其中包含一批本轮人工 QA 截图。

处理原则：

如果这些图片：

```text
没有被 README/docs/比赛材料引用
且
没有作为自动 visual regression baseline
```

则：

# 不要长期提交在仓库。

优先改为：

```text
Playwright / GitHub Actions artifact
```

保存。

如果确实需要展示：

只保留少量精选截图到：

```text
docs/screenshots/
```

不要每轮积累十几张。

---

同时统一：

```text
docs/status/current.md
docs/risks/register.md
README.md
```

中的：

```text
pytest 数量
Playwright 数量
Safety case 数量
```

避免 114 / 116 等状态不一致。

---

# 4. P1-1：恢复医生真实照片

这是当前最值得恢复的旧版能力之一。

仓库中已经保留大量医生图片：

```text
static/images/doctors/
```

数量约 2000+。

医生数据中也存在：

```text
photo_url
source_image_url
doctor_page_url
```

旧版已有：

```text
resolveDoctorPhoto()
doctorAvatarHtml()
```

但新 React 前端现在主要显示：

```text
姓名首字 Avatar
```

---

# 5. 医生照片实现要求

优先使用现有正式数据中的：

```text
photo_url
```

或者经过映射确认的本地图片。

不要重新爬取图片。

逻辑：

```text
photo_url 存在
↓
展示医生照片

加载失败
↓
姓名首字 fallback
```

必须使用：

```text
onError fallback
```

不能让 broken image 出现在 UI。

---

# 6. 医生照片使用位置

至少：

```text
/resources 医生列表
医生详情
推荐结果医生卡片
医院详情中的关联医生
```

保持统一组件。

建议抽出：

```text
DoctorAvatar
```

不要四个页面分别写逻辑。

---

# 7. 照片隐私与来源

只允许使用：

```text
公开医院官网
公开医生主页
仓库已有经过来源记录的资源
```

不要：

```text
从搜索引擎随机抓图
用 AI 生成人脸
```

如果图片来源不能确认：

继续用姓名 Avatar。

---

# 8. P1-2：恢复医院 Logo / 机构图片

仓库当前已有：

```text
static/images/hospitals/
```

包含约几十个 Logo / icon / 图片。

旧版已有：

```text
resolveHospitalLogo()
hospitalLogoHtml()
```

新版医院卡目前主要使用统一 Building 图标。

---

# 9. 医院视觉资产原则

先审计：

```text
医院 ID
→ 图片
→ 来源
→ 是否当前有效
```

只有来源可信的资产才展示。

建议抽：

```text
HospitalLogo
```

统一负责：

```text
真实 Logo
↓
加载失败
↓
Building icon fallback
```

---

# 10. 医院 Logo 使用位置

至少：

```text
医院列表
医院详情
推荐结果医院卡片
地图医院 preview
```

不要让医院 Logo 成为主导视觉。

它只是帮助快速辨识。

---

# 11. P1-3：恢复资源高级筛选

旧版资源页曾支持：

## 医院

```text
医院等级
医院类型
```

## 医生

```text
医院等级
具体医院
科室
职称
关键词搜索
```

现在 React 版基本只有：

```text
搜索框
医院 / 医生 Tab
```

对于：

```text
2100+ 医生
```

搜索框不够。

因此恢复筛选是有价值的。

---

# 12. 新版不要照搬旧后台 UI

不要做：

```text
一排 8 个传统 select
```

推荐：

```text
搜索框

[医院 ▾]
[科室 ▾]
[职称 ▾]

[重置]
```

或者 chips + popover。

---

# 13. 医院筛选

最低支持：

```text
医院等级
医院类型
区域 / 区县（数据可靠时）
是否存在急诊字段
```

但：

```text
“急诊字段存在”
```

不能写成：

```text
“当前急诊可接诊”
```

因为没有实时数据。

---

# 14. 医生筛选

最低支持：

```text
医院
科室
职称
```

可以继续结合当前关键词：

```text
医生名
医院名
科室
专长
```

---

# 15. 筛选只影响浏览，不影响推荐算法

必须保持：

```text
Resources filter
≠
Recommendation ranking
```

产品文案已经表达这一点。

不要把：

```text
用户在 Resources 里筛了某医院
```

偷偷当推荐偏好。

---

# 16. URL 可以保留筛选状态

如果实现简单，可以使用：

```text
/resources?type=doctor&hospital=...&department=...
```

这样方便：

```text
返回
分享
详情恢复
```

不是强制。

---

# 17. P1-4：恢复简化专家偏好 UI

当前后端已经支持：

```text
expert_preference
```

包括：

```text
system
no_expert
wish_expert
must_expert
named_followup
```

但前端没有入口。

这属于：

# 后端能力存在，但产品能力未迁移完成。

---

# 18. 前端只暴露三个简单选项

不要把全部内部模式展示给用户。

建议：

```text
医生资源偏好

● 系统平衡推荐
○ 希望优先专家
○ 不特别需要专家
```

映射：

```text
系统平衡推荐
→ system

希望优先专家
→ wish_expert

不特别需要专家
→ no_expert
```

---

# 19. 不要把专家作为默认最好

产品必须继续表达：

> 专家资源不一定适合所有常见病与初诊场景。

系统默认仍：

```text
system
```

而不是：

```text
wish_expert
```

---

# 20. 专家偏好放在哪里

不要增加巨大患者信息表。

放到：

```text
Triage 结果准备加载资源之前
```

或者：

```text
高级资源偏好
```

折叠区域。

---

# 21. expert preference E2E

至少验证：

```text
system
wish_expert
no_expert
```

请求 body 正确。

并确认：

```text
Safety Gate
```

不受该选项影响。

---

# 22. P1-5：实现真正 Near-duplicate Group Split

当前已有：

```text
Random Split
Exact Fingerprint Grouped Split
```

但是数据报告显示：

```text
Jaccard >= 0.8
近重复 pair ≈ 605
```

Exact fingerprint 无法解决：

```text
症状集合只差 1–2 个词
```

的训练测试泄漏风险。

这是当前模型证据最应该完善的地方。

---

# 23. 建立 Near-duplicate components

建议：

对所有样本 symptom set 计算：

```text
Jaccard similarity
```

如果：

```text
Jaccard >= 0.8
```

建立边。

然后：

```text
Connected Components
```

同一 component：

# 不允许跨 Train / Test。

---

# 24. 防止跨疾病错误合并

需要先分析：

```text
近重复 pair
```

是否存在：

```text
不同疾病
症状高度相似
```

如果存在很多：

不能机械把整个数据全部合并成一个超大 component。

可以比较：

### Split A

```text
same-label near duplicate grouping
```

### Split B

```text
global near duplicate grouping
```

并记录差异。

---

# 25. 至少输出三个评估

```text
Random Split

Exact Fingerprint Split

Near-duplicate Group Split
```

Trust Center 默认更应该强调：

```text
Near-duplicate Group Split
```

如果样本规模允许。

---

# 26. 输出指标

至少：

```text
Top-1
Top-3
Macro Precision
Macro Recall
Macro F1
Per-class Recall
Coverage
```

并输出：

```text
train/test samples
groups/components
cross-split duplicate count
cross-split Jaccard>=0.8 count
```

---

# 27. 如果结果显著下降

例如：

```text
98%
→
75%
```

不要优化数据来把数字拉回去。

真实报告。

Trust Center 明确说明：

> 更严格切分降低了近重复泄漏风险，因此指标更能反映泛化能力。

---

# 28. 不允许 test leakage

禁止：

```text
反复换 seed 选最好
使用 test 调模型参数
根据 test 删除困难类别
```

---

# 29. P1-6：交通数据建立正式 Quality Gate

旧版包含：

```text
公交站
出租车样本
共享骑行
```

当前后端仍保留：

```text
traffic.py
TransitRepository
```

但：

```text
data validation 仍有约 187 个异常
```

所以现在不能简单恢复旧交通 UI。

---

# 30. 先建立 Transit Quality Classification

给当前交通数据集分类：

```text
VERIFIED
PROVISIONAL
INVALID
```

或者类似状态。

依据至少：

```text
source
license
timestamp
coordinate validity
schema validity
time-order validation
placeholder checks
```

---

# 31. Ranking gate

只有：

```text
rankable = true
```

的交通字段：

才能进入 recommendation ranking。

否则：

```text
transit feature unavailable
```

并降级到：

```text
直线距离
```

---

# 32. 当前 masked/sample 数据要非常谨慎

类似：

```text
derived_masked_sample
public_secondary_masked
```

默认不要给过高权重。

如果没有明确质量证明：

应该：

```text
展示参考
≠
正式排名依据
```

---

# 33. 修数据时禁止批量猜测

不要：

```text
自动修复 187 个异常
```

尤其不要凭规律生成：

```text
时间
坐标
站点
```

缺失值。

不确定：

保持 null / invalid。

---

# 34. P1-7：恢复可信的交通可达性解释

完成 Quality Gate 后，才允许恢复部分交通产品能力。

不要恢复旧版：

```text
公交大屏
大量 Dashboard
```

推荐结果只增加：

```text
交通可达性
```

解释。

例如：

```text
距你约 3.2 km
最近公共交通站点约 420 m
附近公共交通资源较多
```

只有数据可靠时展示。

---

# 35. 交通信息必须说明来源

例如：

```text
基于当前已接入公共交通站点数据估算
```

不要写：

```text
预计 12 分钟到达
```

除非真的接实时路线 API。

---

# 36. Map 可选恢复公交图层

如果：

```text
公交站点数据通过 quality gate
```

可以在地图增加：

```text
[医院]
[公交站]
```

图层开关。

不建议本轮恢复：

```text
单车车辆点
出租车动态点
```

除非数据非常可靠。

---

# 37. P1-8：恢复简单安全的语音输入

旧版已有：

```text
SpeechRecognition
```

但新版已经正确删除未实现的 disabled 按钮。

本轮可以重新实现，但要简化。

---

# 38. 语音只做 Speech → Text

流程：

```text
点击“语音输入”
↓
浏览器 SpeechRecognition
↓
生成文字
↓
写入症状输入框
↓
用户人工检查
↓
用户点击分析
```

不要：

```text
识别完自动提交
```

---

# 39. 不要恢复旧版复杂语音自动填表

旧版可以自动抽：

```text
年龄
性别
医保
区域
疾病
就诊时间
```

本轮不要。

因为这会引入：

```text
解析错误
医疗语义错误
隐私问题
```

先只做：

```text
Speech → Editable Text
```

---

# 40. 浏览器不支持时

隐藏或 disabled：

```text
语音输入
```

但用户体验文案：

```text
当前浏览器不支持语音输入
```

不要：

```text
开发中
后续切片
```

---

# 41. 语音权限

必须：

```text
用户主动点击
```

才请求 microphone。

不能页面加载自动弹窗。

---

# 42. P1-9：医生/医院浏览体验完善

医生照片和医院 Logo 加入后：

重点检查卡片不要变得像：

```text
医院官网列表
```

仍保持现有：

```text
Warm Precision
Editorial Healthcare
```

设计。

---

# 43. 医生详情可以恢复更多公开资料

如果当前 API 已经返回并且来源明确：

可以展示：

```text
公开擅长
职称
门诊时间
公开个人简介
科研资料
公开主页链接
```

但必须继续区分：

```text
科研资料
≠
patient-fit ranking evidence
```

---

# 44. 医院详情

只展示可靠：

```text
等级
类型
地址
电话
科室
急诊字段
关联医生
来源
更新时间
```

不要重新显示旧：

```text
rating 4.7
beds
daily_outpatients
96 分
```

等未经核验值。

---

# 45. 暂时不要恢复收藏

旧版：

```text
优推医生
```

本轮不做。

原因：

它不是当前主链路。

写入 P2：

```text
Local favorites / watchlist
```

未来可以恢复。

---

# 46. 暂时不要恢复完整推荐历史

当前：

```text
本地脱敏摘要
```

继续保持。

不要重新保存：

```text
完整症状
疾病详情
聊天内容
完整推荐
```

除非以后有：

```text
正式隐私设计
账号
加密
```

---

# 47. 不恢复浮动 Assistant

明确：

```text
不要恢复
```

项目定位保持：

```text
AI Care Routing Product
```

不是：

```text
ChatGPT wrapper
```

---

# 48. 不恢复 Dashboard

明确：

```text
不要恢复
```

Trust Center 已经承担技术证据展示。

---

# 49. P1-10：移动端与浏览器 QA

当前 E2E 虽然已经存在，但移动端覆盖不足。

至少增加：

```text
Home mobile
Triage mobile
Routine Result mobile
Emergency mobile
Resources mobile
Hospital Detail mobile
Doctor Detail mobile
Map mobile
Trust mobile
```

---

# 50. 测试定位拒绝

Playwright：

```text
permission denied
```

验证：

```text
页面正常
可以选择区域
可以继续不提供位置
```

---

# 51. 测试语音不作为 CI 必需能力

因为：

```text
SpeechRecognition
```

在 headless Chromium 中支持可能不一致。

只测试：

```text
unsupported fallback
UI behavior
```

语音真实录音采用人工 browser QA。

---

# 52. Visual QA

这次不要重新往 Git 根目录加 13 张截图。

CI 自动上传：

```text
playwright-report
qa-screenshots
```

Artifact。

---

# 53. 如果做 Visual Regression

只给关键稳定页面保存 baseline：

```text
home
triage-initial
routine-result
emergency
resources
map
trust
```

不要给所有交互状态建立大量 snapshot。

---

# 54. 浏览器矩阵

至少：

```text
1440×900
1280×800
768×1024
390×844
```

---

# 55. 重点检查

```text
图片加载失败
布局跳动
长医生名
长科室名
筛选组合
地图 resize
sticky
drawer
移动端横向溢出
```

---

# 56. 当前 Safety 不准退化

继续保持：

```text
38 cases
Recall 1.0
Under-triage 0
Over-triage 0
Emergency FN 0
```

这些只是固定工程样例。

不要写：

```text
100% 临床安全
```

---

# 57. 本轮原则：不要修改 Safety Rules

除非发现明确回归。

如果需要改：

单独停止当前 scope，并按 L3 医疗变更流程处理。

---

# 58. Recommendation ranking 不要重新设计

上一轮已经完成：

```text
legacy hospital score
→ 不参与正式排序

academic
→ 弱化
```

本轮不要再次随意调权重。

---

# 59. Academic 权重可以做一次最终决策

当前：

```text
academic = 0.02
```

本轮审计后可以选择：

### 保持 0.02

作为非常弱的 tie-break。

或：

### 设为 0

彻底退出 patient-fit。

但不要重新提升。

---

# 60. 如果保持 academic=0.02

文档明确：

```text
科研指标只作为极弱排序 tie-break，
不解释为临床能力或疗效。
```

---

# 61. 添加功能迁移记录

更新：

```text
docs/POST_REFACTOR_BACKLOG.md
```

将旧功能重新分类：

## 已恢复

```text
高德导航
真实地图
用户定位
医院详情
医生详情
医生照片
医院 Logo
资源高级筛选
专家偏好
```

完成后才标。

---

## 延后

```text
语音（如果本轮未完成）
交通地图层
收藏
```

---

## 永久不恢复

```text
Demo Login
Dashboard
浮动 Assistant
Tester Feedback
自动轮播
```

---

# 62. 不要把 Prompt 提交进仓库

非常重要：

# 不要创建新的 `1.md`。

也不要创建：

```text
prompt.md
task.md
mimo-task.md
```

只为了保存本轮 Prompt。

Prompt 不属于正式项目。

---

# 63. 文档控制

本轮不需要新建大量：

```text
plan
progress
history
review
```

如果确实需要：

只创建：

```text
docs/plans/P1_PRODUCT_COMPLETION.md
```

一份。

最后更新：

```text
docs/status/current.md
docs/POST_REFACTOR_BACKLOG.md
docs/risks/register.md
```

即可。

---

# 64. 推荐执行顺序

严格按：

```text
1. 清理 1.md / QA artifacts
2. 同步状态文档
3. 恢复医生照片
4. 恢复医院 Logo
5. 增加资源高级筛选
6. 增加专家偏好 UI
7. 实现 Near-duplicate Group Split
8. 更新 Trust Center
9. 建立 Transit Quality Gate
10. 视数据质量恢复交通可达性解释
11. 恢复简单语音输入
12. 扩展移动 E2E
13. 浏览器 QA
14. 全量 CI
15. 文档收口
```

---

# 65. 如果任务变得过大

可以拆成三个逻辑 commit group：

## Product UX

```text
photos
logos
filters
expert preference
```

## Evidence

```text
near-duplicate split
trust
transit quality gate
```

## Accessibility / QA

```text
voice
mobile E2E
visual QA
cleanup
```

但不要改变总体 scope。

---

# 66. Git 建议

逻辑提交示例：

```text
chore: remove prompt and transient qa artifacts

feat: restore verified medical resource imagery

feat: add resource filtering and expert preference controls

eval: add near-duplicate grouped model evaluation

fix: gate transit ranking by data quality

feat: add safe speech-to-text symptom input

test: expand responsive product smoke coverage
```

---

# 67. 不要 push

除非用户明确授权：

```text
不要 git push
```

可以 commit。

---

# 68. 最终验收

完成后必须满足：

## 仓库

```text
没有 1.md
没有 Agent prompt artifact
没有无意义 QA 图片堆积
```

## 医生

```text
可靠照片可显示
加载失败有 fallback
```

## 医院

```text
可靠 Logo 可显示
无来源资产不会强行展示
```

## Resources

```text
医院可筛
医生可按医院/科室/职称筛
搜索和筛选可以组合
```

## Expert Preference

```text
system
wish_expert
no_expert
```

前端可操作。

## Model

```text
Random
Exact Group
Near-duplicate Group
```

三种评估明确区分。

## Transit

```text
未通过质量门的数据不得强参与 ranking
```

## Voice

```text
Speech → Text → 用户确认 → 提交
```

不能自动医疗分析。

## QA

```text
Desktop + Tablet + Mobile
```

核心链路通过。

---

# 69. 完整测试

运行：

```bash
.venv/bin/python -m pytest
.venv/bin/python -m evaluation.safety.evaluate_safety
.venv/bin/python -m data_validation.validate_datasets --data-root data --output-dir data_validation

cd frontend
npm run typecheck
npm run test
npm run build
npm run e2e
```

再运行新的模型评估。

---

# 70. 最终报告格式

最后向用户只报告真实结果：

```text
P1 Product Completion

1. Repository cleanup
- removed:
- retained:

2. Medical resource imagery
- doctor photos:
- hospital logos:
- fallback:

3. Resource filters
- hospital:
- doctor:

4. Expert preference
- modes exposed:
- backend mapping:

5. Model evaluation
- Random:
- Exact Group:
- Near-duplicate Group:
- cross-split near duplicates:

6. Transit
- datasets:
- quality status:
- used in ranking:

7. Voice
- supported browsers:
- fallback:

8. Browser QA
- desktop:
- tablet:
- mobile:

9. Tests
- pytest:
- Safety:
- frontend:
- Playwright:

10. Remaining P2
```

---

# 71. 最重要的最终原则

> **只恢复真正提升新版产品价值的旧能力。**

> **不要追求“旧项目功能 100% 搬运”。**

> **产品能力恢复不能牺牲当前已经建立的医疗安全、数据可信度和架构边界。**

> **医生照片、医院 Logo 和筛选是产品体验；它们不能反过来成为推荐算法依据。**

> **交通数据必须先可信，再进入推荐和地图。**

> **Near-duplicate evaluation 的可信度，比维持 98% 的漂亮数字更重要。**

现在开始执行。
