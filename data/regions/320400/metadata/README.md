# 320400 常州 Region Pack

这是首轮迁移的区域边界，不是新的数据来源。当前医生和交通文件通过 manifest 以只读方式引用仓库中的既有资产；医院目录仍标记为 `migration_pending`，因为 `app.py` 中的字段需要逐项完成来源与派生语义审计后才能迁入正式数据文件。

激活条件：来源、许可/使用权限、采集或验证时间、schema、脱敏状态和自动质量检查全部可追溯。没有满足条件的数据不能进入正式主推荐结果。
