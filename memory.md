# Memory — 重构工作流初始化

Last updated: 2026-09-09

## What was built

建立了仓库级 `AGENTS.md`、根 `README.md` 和 `docs/` 工作流体系，包含项目章程、当前架构、决策记录、历史事件、进度、重构路线图、风险登记、质量门禁、复核入口和记录模板。

## Decisions made

- 采用“规则 + 记录 + 小步重构 + 质量门禁”的协作流程。
- 医疗输出只作为辅助信息；红旗、低置信度、输入不足和模型不可用必须安全降级。
- 重构采用先建立行为基线、再渐进式拆分单体的路线，不做一次性大爆炸重写。
- 当前首要阶段是治理和行为基线，不立即改变业务代码。

## Problems solved

项目原先缺少根入口文档、统一的变更分级、风险登记、重构计划和跨会话交接规则；这些入口已补齐。

## Current state

工作流文件已写入工作区但尚未提交。业务代码未修改。当前项目仍是 Flask 后端单体加单体前端，暂无正式自动化测试或 CI。项目已有的 `skills/` 目录已阅读但仍是未跟踪目录，是否纳入版本控制尚未决定。

## Next session starts with

先读取 `AGENTS.md`、本文件、`docs/status/current.md` 和 `docs/plans/refactor-roadmap.md`，然后执行 Phase 0：建立 API、关键纯函数、模型降级和数据加载的最小行为基线。

## Open questions

- `skills/` 是否作为项目协作资产纳入版本控制。
- 医院/医生数据的正式发布范围、来源和许可口径。
- 红旗症状与分诊等级是否需要指定医学审核者确认。
