你要继续完成仓库：

`https://github.com/wenxvn/changyi`

的**剩余重构工作**。

这不是新一轮功能开发。

本轮唯一目标是：

> **把已经开始但尚未彻底完成的重构收尾，完成新旧架构切换，验证行为一致后删除无用 legacy 代码、文件和重复过程文档，把仓库整理成一个清晰、稳定、可继续开发的新基线。**

完成本轮之前：

# 禁止增加任何新产品功能。

---

# 0. 第一原则

本轮工作可以概括成：

```text
恢复上下文
↓
确认当前真实状态
↓
完成已有重构
↓
验证新旧行为一致
↓
切换新架构为正式入口
↓
删除 Legacy / Dead Code
↓
精简文档和工作流
↓
全量验证
↓
形成新的干净基线
```

不要：

```text
一边重构
一边继续加页面
一边继续加接口
一边继续加医学能力
```

本轮结束之前，功能冻结。

---

# 1. 开始前必须读取的文件

不要根据之前对话记忆猜测进度。

首先读取：

```text
AGENTS.md
README.md
memory.md

docs/README.md
docs/status/current.md

docs/progress/2026-09-11-refactor-handoff.md

docs/plans/refactor-roadmap.md
docs/competition/2026-ai-medical/MIGRATION_PLAN.md

docs/architecture/current-state.md

docs/decisions/
docs/risks/register.md
docs/quality/quality-gates.md

ui-registry.md
```

尤其以：

```text
docs/progress/2026-09-11-refactor-handoff.md
```

作为上一轮暂停时的核心交接依据。

然后检查真实代码：

```text
app.py
backend/
frontend/

templates/
static/js/
static/css/

tests/
evaluation/
data_validation/

.github/workflows/
```

开始前运行：

```bash
git status --short --branch
git log --oneline -10
git diff
```

当前 `main` 最新基线应先核实是否仍然是：

```text
35b4f3d
Refactor application architecture and improve maintainability
```

如果文档中的“基线提交”与 Git 实际 HEAD 不一致：

**以 Git 当前代码为事实来源，并立即修正文档。**

---

# 2. 已知当前状态

不要重复已经完成的工作。

当前已经完成：

## 新前端

已经存在：

```text
frontend/
```

技术栈：

```text
React
TypeScript
Vite
```

已经包含首版：

```text
Homepage
Triage
Follow-up
Routine Result
Urgent Result
Emergency Result
Resources
Hospital / Doctor Detail
Map
Trust
Profile / History
Responsive Shell
Accessibility
```

已经有：

```text
集中 API client
strict TypeScript
Design Tokens
Warm Precision UI
React 页面状态
```

不要重新搭建前端项目。

不要重新设计一套视觉方向。

不要继续添加页面。

---

# 3. 已完成的后端接缝

已经建立：

```text
backend/app/application/
backend/app/domain/
backend/app/infrastructure/
backend/app/api/v1/
```

包括但不限于：

```text
TriageApplicationService
RecommendationApplicationService
ResourceCatalogApplicationService
DiseasePredictionApplicationService
TransitCatalogApplicationService
DistanceRerankApplicationService
SummaryApplicationService
EvidenceApplicationService
MapViewApplicationService
RegionReadApplicationService
SymptomDiseaseModelAdapter
```

以及：

```text
Safety Gate
Safety-first publication
Recommendation domain
Resource policy
Traffic domain
RegionRegistry
Repositories
v1 handler registry
legacy route adapters
```

不要重新实现这些。

本轮重点是：

> **完成剩余边界迁移，并最终让 legacy compatibility 不再成为主体架构。**

---

# 4. 当前真正没有完成的事情

根据交接记录和当前代码，主要剩余问题分成 5 类。

---

# A. app.py 仍然太重

当前 `app.py` 仍然大约 2500 行，并仍存在大量：

```text
医学组合逻辑
legacy route
数据加载
交通组合
推荐组合
兼容 wrapper
HTTP adapter
```

虽然很多内部逻辑已经迁出，但：

> `app.py` 目前仍然是 legacy application + composition root 的混合体。

本轮必须继续拆完。

---

# B. Legacy Route Parity 尚未完成

当前仍存在大量：

```text
/api/*
```

legacy route。

新：

```text
/api/v1/*
```

已经承担主要竞赛前端需求，但旧路由仍承担兼容职责。

必须：

```text
盘点
↓
验证
↓
迁移
↓
确认调用方
↓
删除无用 legacy route
```

不能无限保留 adapter。

---

# C. 新 React 前端还不是正式默认入口

目前：

```text
/
```

仍然是：

```text
templates/index.html
static/js/app.js
static/css/style.css
```

而：

```text
frontend/
```

仍属于并行迁移。

这意味着：

> 前端重构事实上还没有真正结束。

本轮必须完成正式 cutover。

---

# D. E2E / Cutover 验证不足

已有大量：

```text
pytest
API smoke
boundary test
typecheck
build
browser smoke
```

但正式 cutover 前，还需要建立最终核心流程验证。

---

# E. 仓库中存在大量重构过程垃圾

当前仓库存在大量：

```text
docs/plans/YYYY-MM-DD-*.md
docs/progress/YYYY-MM-DD-*.md
docs/history/YYYY-MM-DD-*.md
```

很多内容只是同一个 Slice 的：

```text
Plan
Progress
History
```

三份重复记录。

这是重构过程中有帮助的临时过程资产。

但不能成为最终项目结构。

本轮完成之后必须精简。

---

# 5. 本轮严格禁止的新功能

以下内容即使出现在旧 roadmap 或 handoff 的“下一步”中：

# 本轮也不要做。

包括：

```text
正式附近急诊导航
实时急诊可用性
导航路径规划
结构化 follow-up answer 新 API
新的疾病模型
重新训练模型
修复 Safety Evaluation 医学规则
调整 Red Flag
调整分诊阈值
调整推荐权重
新的医院排序算法
新的医生排序算法
新的推荐特征
新城市
全国化数据
新的医院/医生数据源
账号系统
云同步
数据库
生产认证
OAuth
真实患者档案
部署系统
新 Agent
LLM
语音识别
新地图能力
新的 Trust 指标
新的 Profile 功能
新的 UI 页面
新的动画/视觉功能
```

这些全部进入：

```text
POST_REFACTOR_BACKLOG
```

而不是现在实现。

---

# 6. 医学行为必须冻结

本轮是结构重构。

因此以下内容必须保持：

```text
红旗规则
分诊状态
疾病预测
推荐权重
候选规则
医院排序
医生排序
资源策略
Emergency publication
Routine publication
Urgent publication
```

禁止为了让测试变好而修改医学行为。

---

# 7. Safety Evaluation 必须保持原基线

当前基线：

```text
Cases: 16

Red Flag Recall:
0.9231

Under-triage:
0.0769

Over-triage:
0.0

Emergency False Negative:
1
```

这个结果目前并不理想。

但：

# 本轮不要修它。

因为修复属于医学行为变更，不属于重构。

只需要保证：

> 重构前后的 Safety 行为没有意外变化。

如果指标发生变化：

立即判断为 regression。

不要静默接受。

---

# 8. Recommendation Snapshot 必须保持

当前 characterization snapshot SHA-256：

```text
f25776066ed2c9664fadffec2b34a71e11bd28e7c232c87780081e41aa0bac9d
```

纯重构过程中必须保持。

如果最后由于删除 legacy harness 而需要删除旧 snapshot：

必须先：

```text
用新的 v1 contract / golden test
证明关键业务结果一致
```

之后才允许移除旧 snapshot。

---

# 9. 第一阶段：建立“最终重构清单”

开始修改前先生成一个短期清单。

不要再创建几十个 Slice 文档。

只建立一个：

```text
docs/plans/FINAL_REFACTOR.md
```

里面只包含：

```text
1. Backend completion
2. API parity
3. Frontend cutover
4. Validation
5. Legacy removal
6. Repository cleanup
7. Documentation cleanup
```

以后整个本轮只维护这一份计划。

# 禁止为每个小步骤再新建一份 plan/progress/history。

---

# 10. 第二阶段：盘点 app.py

对 `app.py` 中所有内容分类。

建议建立临时表：

```text
Symbol / Route
Current owner
Target owner
Still used?
Migration status
Can delete?
```

分类：

```text
HTTP Adapter
Application Orchestration
Domain Logic
Infrastructure
Compatibility Wrapper
Dead Code
```

目标：

最终 `app.py` 不应该继续承载：

```text
医学规则实现
推荐规则实现
交通计算
模型实现
资源目录业务逻辑
复杂结果组装
```

最终它最多承担：

```text
应用启动
create_app
dependency composition
必要的 Flask bootstrap
```

如果仍保留：

```text
legacy compatibility
```

必须说明为什么。

---

# 11. 完成剩余医学组合逻辑迁移

注意：

这是“搬运”，不是“改变”。

目前仍在 legacy 中的：

```text
known disease composition
red flag composition
follow-up composition
部分 triage orchestration
```

可以继续迁移到已有：

```text
domain
application
```

边界。

但是：

# 不改变任何规则和文本语义。

先 characterization。

再移动。

再比较。

---

# 12. 完成 Recommendation 剩余编排迁移

当前已抽出大量：

```text
candidate
features
scoring
resource_policy
traffic
pipeline
application service
```

不要重新设计。

只处理仍然残留在 `app.py` 中的：

```text
候选 orchestration
急症整体排序 orchestration
交通读取 wiring
legacy composition
```

目标：

```text
HTTP
↓
Application Service
↓
Domain
↓
Infrastructure
```

而不是：

```text
HTTP
↓
app.py 200 行逻辑
↓
Domain
```

---

# 13. 完成交通边界

当前交通：

```text
repository
traffic domain
TransitCatalogApplicationService
cache seam
```

已经存在。

本轮只完成：

```text
旧加载代码归位
缓存生命周期归位
重复逻辑删除
```

不要增加：

```text
实时交通
导航
路线规划
急诊实时路径
```

---

# 14. Resource Catalog 收口

当前：

```text
ResourceCatalogApplicationService
```

已经是主要资源目录边界。

确认：

```text
医院 index
医生 index
医院 detail
医生 detail
医院医生 relation
department
district
stats
```

是否仍存在两套实现。

如果存在：

保留一个 canonical implementation。

删除重复实现。

---

# 15. Prediction 收口

当前已经存在：

```text
DiseasePredictionApplicationService
SymptomDiseaseModelAdapter
```

因此：

```text
app.py
```

不应再存在另一套完整模型加载 / 推理实现。

兼容 wrapper 可以暂时存在。

当所有调用迁移完成后：

删除 wrapper。

---

# 16. API Route Inventory

列出当前所有旧接口。

至少检查：

```text
/api/hospitals
/api/hospitals/<id>
/api/doctors
/api/doctors/<id>

/api/predict-disease

/api/triage
/api/followup
/api/assistant/process

/api/recommend
/api/recommend/enhanced
/api/recommend/rerank

/api/hospitals/<id>/doctors
/api/departments
/api/districts
/api/stats

/api/transit/*

/api/test-feedback
/api/test-record
```

以及真实仓库中其他 route。

---

# 17. 每个 Legacy Route 必须分类

每个旧接口最终只能属于：

### A. Still Required

仍有明确调用方，需要临时兼容。

### B. Superseded

已有：

```text
/api/v1
```

完全替代。

### C. Dead

已经没有任何调用方。

---

# 18. 判断调用方必须使用真实搜索

删除 route 前必须检查：

```bash
git grep
```

至少检查：

```text
frontend/
static/
templates/
tests/
scripts/
docs/
```

不能凭感觉删。

---

# 19. 新前端只使用 v1

最终：

```text
frontend/
```

不得再依赖 legacy：

```text
/api/*
```

业务接口。

所有正式调用使用：

```text
/api/v1/*
```

如果发现仍然调用 legacy：

改成已有 v1 equivalent。

注意：

# 不为此设计新业务功能。

仅做 contract migration。

---

# 20. test-feedback / test-record

重点检查：

```text
/api/test-feedback
/api/test-record
```

如果它们：

```text
只服务旧前端
新前端不用
没有比赛必须需求
```

那么在 legacy UI 删除之后：

一起删除。

包括：

```text
route
JSONL write logic
相关临时文件
相关未使用测试
```

不要把这两个 endpoint 顺手升级成数据库功能。

---

# 21. 第三阶段：完成 React Frontend Cutover

这是本轮重要完成条件。

当前：

```text
legacy /
```

必须最终切换到：

```text
frontend/
```

的新 React 页面。

完成后：

用户运行项目：

```bash
python app.py
```

应该能够打开正式新版界面。

不要要求普通演示流程必须：

```text
Terminal 1 Flask
Terminal 2 npm dev
```

开发模式可以这样。

正式 demo 不应该如此。

---

# 22. Production Frontend Serving

选择当前架构最简单可靠的方式：

例如：

```text
Flask serving frontend build
```

或者仓库已有更合适的模式。

原则：

```text
一条正式启动路径
一个正式前端
```

不要建立新的部署系统。

---

# 23. 保留 SPA Route

需要确保：

```text
/
/triage
/resources
/map
/trust
/profile
```

刷新时不会 404。

如果 React router 需要 fallback：

在 Flask 静态服务层解决。

这属于 cutover，不是新功能。

---

# 24. 新前端 Cutover 后重新验证

核心三条流程：

## Routine

输入：

```text
最近皮肤一直很痒，大概一周，没有呼吸困难，也没有发烧
```

检查：

```text
Routine
非 Emergency
正常资源结果
```

---

## Insufficient

输入：

```text
最近总是头晕
```

检查：

```text
Follow-up
信息不足路径
```

---

## Emergency

输入：

```text
胸口压榨样疼痛，喘不过气，还一直冒冷汗
```

检查：

```text
Emergency
不请求普通推荐
显示安全提示
保留 tel:120
```

不要修改上述行为。

---

# 25. 新前端与旧前端行为对齐

这里的 parity 指：

```text
核心能力
Safety semantics
API result meaning
error handling
```

不是：

```text
像素级复制旧 UI
旧后台功能全部照搬
```

旧后台有一些：

```text
管理台 UI
图表
demo login
旧数据面板
```

如果新竞赛前端已经明确不需要：

# 不要为了 parity 把它重新实现出来。

这些属于 legacy 产品形态。

---

# 26. 完整 E2E

本轮不加新业务功能。

但测试工具不属于产品功能。

如果环境允许：

接入：

```text
Playwright
```

只覆盖现有核心流程。

最低：

```text
Home loads

Home -> Triage

Routine

Insufficient -> Follow-up

Emergency

Resources

Hospital detail

Doctor detail

Map

Trust

Profile

404 / API failure basic fallback
```

不要为了写 E2E 扩展产品。

---

# 27. Visual Smoke

不需要继续设计新 UI。

只检查当前 UI：

```text
1440×900
1280×800
768×1024
390×844
```

核心：

```text
Home
Triage
Routine
Emergency
Resources
Map
Trust
```

检查：

```text
overflow
broken layout
missing asset
console error
network 404
focus
keyboard
reduced motion
```

发现 bug：

修 bug。

不要顺便重做设计。

---

# 28. CI 收口

确认现有：

```text
.github/workflows/quality.yml
```

真正覆盖：

```text
Python compile
pytest
Safety baseline
data validation
characterization
frontend install
typecheck
test
build
```

如果加入 Playwright：

可增加最小 smoke。

不要建立第二套重复 CI。

---

# 29. 确认远端 CI

交接记录中：

```text
远端首次运行待确认
```

因此本轮如果环境和权限允许：

确认 GitHub Actions 真实运行结果。

若失败：

只修与当前重构相关的问题。

---

# 30. 第四阶段：Legacy Removal

只有满足：

```text
新版成为默认入口
核心流程通过
v1 API 可用
Safety baseline 未改变
Recommendation golden 未改变
frontend build 通过
browser smoke 通过
```

以后：

才进入删除阶段。

---

# 31. 删除 Legacy Frontend

首先检查：

```text
templates/index.html
static/js/app.js
static/css/style.css
```

以及：

```text
legacy-only JS
legacy-only CSS
legacy-only images
legacy-only Leaflet assets
```

如果已经没有正式调用：

删除。

最终不能继续同时维护：

```text
React frontend
+
4351 行 legacy JS
+
7174 行 legacy CSS
```

两套前端。

---

# 32. 不要保留“以后可能有用”

判断文件是否保留：

```text
当前运行需要？
当前测试需要？
当前构建需要？
当前数据 pipeline 需要？
当前比赛材料需要？
```

如果五个答案都是：

```text
否
```

则删除。

---

# 33. 静态文件清理

重点检查当前仓库中的备份目录，例如：

```text
static/images/hospitals/_logo_backup_20260605_180711/
static/images/hospitals/_remaining_logo_backup_20260605_181357/
```

如果：

```text
无代码引用
无数据 pipeline 需要
无比赛证据价值
```

直接删除。

Git 已经保存历史。

仓库不需要继续当备份盘。

---

# 34. cloudflared.exe

当前存在：

```text
tools/cloudflared.exe
```

检查：

```bash
git grep cloudflared
```

如果没有：

```text
脚本
CI
正式运行流程
README
```

依赖：

删除。

不要在代码仓库保存无用 Windows 二进制。

---

# 35. root doctors.json

检查：

```text
doctors.json
```

是否只是旧 fallback / duplicate。

如果正式 canonical 数据已经在：

```text
data/
```

并且所有 runtime/test 已迁移：

删除根目录重复文件。

如果仍然是 fallback：

先完成迁移，再删。

不能直接删。

---

# 36. 旧 Leaflet / 地图资源

如果新 React Map 不再使用 legacy Leaflet：

检查：

```text
static/js/leaflet.js
static/css/leaflet.css
Leaflet image assets
```

确认无引用后删除。

如果仍在使用：

保留。

---

# 37. Dead Python

对：

```text
app.py
backend/
scripts/
```

进行 dead-code audit。

查：

```text
未使用 wrapper
未使用 helper
过渡 adapter
重复 constant
旧 data loader
旧 model loader
```

删除前必须：

```text
search reference
run test
```

---

# 38. Legacy Adapter 不能永远存在

例如：

```text
legacy_adapter
compat wrapper
fallback import
```

它们是迁移工具。

如果最终：

```text
所有 retained route
```

都已经走新的 composition：

则删除旧 fallback。

否则：

明确剩余原因。

不能因为“怕删”把所有过渡层永久保留。

---

# 39. 删除 obsolete tests

不要盲目追求测试数量。

例如旧前端被删除后：

专门测试旧 HTML 字符串的测试可以删除。

但是：

必须用新版相同职责测试替代。

原则：

```text
删除 implementation-specific legacy test
保留 behavior coverage
```

---

# 40. 测试数量下降不是问题

当前大约：

```text
116 pytest
```

最终可能：

```text
110
100
90
```

都不是问题。

关键是：

```text
核心行为覆盖没有下降。
```

不要为了维持“116”这个数字保留垃圾测试。

---

# 41. 第五阶段：工作流和文档大清理

这是本轮必须完成的工作。

当前工作流已经过度记录。

出现大量：

```text
plans/
progress/
history/
```

同一 Slice 三份记录。

长期会严重增加 Agent 读取成本和上下文污染。

---

# 42. 重构完成后采用新的轻量工作流

新的长期工作流应该变成：

```text
README
↓
AGENTS
↓
Current Status
↓
Architecture
↓
必要 ADR
↓
Risk / Quality
```

不要每一次小改动都创建：

```text
Plan
Progress
History
Review
Memory
```

五份文档。

---

# 43. 保留的核心文档

最终建议保留：

```text
README.md

AGENTS.md

docs/README.md

docs/status/current.md

docs/architecture/current-state.md
或最终改名 final-state.md

docs/plans/refactor-roadmap.md
```

重构结束后：

`refactor-roadmap.md` 标记：

```text
Completed
```

并缩短为高层总结。

---

# 44. ADR 保留

```text
docs/decisions/
```

中的 ADR 属于长期有价值信息。

保留真正影响未来开发的：

```text
架构决策
安全决策
数据边界
frontend migration
profile privacy
```

如果存在纯过程性 ADR：

可以合并或删除。

但不要丢失关键设计原因。

---

# 45. Risk 保留

保留：

```text
docs/risks/register.md
```

删除：

```text
已经关闭且毫无长期价值的重复 risk 文档
```

如果只有一个 register：

继续保持一个即可。

---

# 46. Quality 保留

保留一个：

```text
docs/quality/quality-gates.md
```

未来测试方法写这里。

不要为每次测试另建一份文档。

---

# 47. Plans 清理

当前：

```text
docs/plans/
```

拥有大量已经完成的日期 Slice。

重构完成后：

先提取仍有长期价值的信息。

然后：

# 删除已经完成的逐 Slice plan。

最终 `plans/` 最多保留：

```text
refactor-roadmap.md
POST_REFACTOR_BACKLOG.md
```

以及真正仍未开始的重要计划。

---

# 48. Progress 清理

当前：

```text
docs/progress/
```

包含大量日期 snapshot。

这些主要用于 Agent 跨会话。

重构结束后：

不需要继续全部存在。

将关键结果合并到：

```text
docs/status/current.md
```

和：

```text
docs/REFACTOR_CHANGELOG.md
```

然后删除绝大多数：

```text
docs/progress/YYYY-MM-DD-*.md
```

包括当前 handoff：

```text
2026-09-11-refactor-handoff.md
```

在本轮真正完成后：

它已经失去作用。

可以删除。

---

# 49. History 清理

当前：

```text
docs/history/
```

记录了大量逐 Slice 完成事件。

将真正有意义的重大里程碑合并成一份：

```text
docs/REFACTOR_CHANGELOG.md
```

例如：

```text
2026-09-09
建立 baseline

2026-09-10
Recommendation/domain extraction
React migration started

2026-09-11
Application service extraction
Frontend parity

最终
React cutover
Legacy removal
```

然后删除重复 history 文件。

---

# 50. Review 清理

只保留：

```text
最终 Review
```

和仍有开放问题的 review。

已经完成并且内容已进入：

```text
current status
ADR
risk
```

的 review：

删除。

---

# 51. docs/templates

检查：

```text
docs/templates/
```

如果新轻量工作流不再要求自动生成：

```text
decision-record
event-record
feature-plan
incident-record
progress-snapshot
```

那么删除未使用模板。

如果保留：

最多留下真正会继续使用的：

```text
ADR template
incident template
```

不要保留整个流程机器。

---

# 52. skills 清理

当前存在：

```text
skills/architect
skills/imprint
skills/recover
skills/remember
skills/review
```

逐个检查是否仍然值得长期保留。

原则：

如果只是为了当前重构流程临时使用，并且：

```text
未来普通开发不会使用
```

可以删除。

不要为了“Agent 很专业”保留没有实际价值的 workflow machinery。

如果保留：

AGENTS 只引用真正仍存在的 skill。

---

# 53. memory.md 精简

当前 `memory.md` 已经逐渐变成过程日志。

最终应该压缩成：

```text
Project
Architecture
Key constraints
Safety invariants
Current baseline
Next product work
```

不要记录几十个 Slice。

目标：

下一位 Agent 阅读 1–2 分钟即可恢复上下文。

---

# 54. ui-registry.md 精简

现在 `ui-registry.md` 包含大量：

```text
migration notes
legacy notes
slice history
```

重构结束后：

只保留当前仍有效的：

```text
tokens
component patterns
spacing
typography
states
accessibility
motion
```

删除 legacy migration 历史。

---

# 55. Competition Docs 也要去重

当前：

```text
docs/competition/2026-ai-medical/
```

存在：

```text
ARCHITECTURE_TARGET
AUDIT_REPORT
COMPETITION_BRIEF
DATA_PROVENANCE
DEMO_SCRIPT
DESIGN_SYSTEM
EVALUATION_PLAN
FRONTEND_ARCHITECTURE
FRONTEND_SCORECARD
MIGRATION_PLAN
MODEL_CARD
PRODUCT_SPEC
RELEASE_CHECKLIST
SAFETY_CARD
SCORECARD
UI_AUDIT_BASELINE
...
```

不要全部无脑删除。

逐份判断。

重构完成后：

例如：

```text
MIGRATION_PLAN
UI_AUDIT_BASELINE
旧 FRONTEND_ARCHITECTURE migration 部分
```

如果内容已经完全历史化：

合并到 final architecture / changelog 后删除。

---

# 56. 建议最终比赛文档只聚焦

长期真正有用的大致是：

```text
README
PRODUCT_SPEC
COMPETITION_BRIEF
DESIGN_SYSTEM
DATA_PROVENANCE
MODEL_CARD
SAFETY_CARD
EVALUATION
DEMO_SCRIPT
RELEASE_CHECKLIST
SCORECARD
```

不要留下 10 份迁移过程说明给评委或下一个 Agent 阅读。

---

# 57. 重写 AGENTS.md

完成重构后：

精简 `AGENTS.md`。

当前规则在重构阶段非常合理。

但以后不再要求：

```text
每个 Slice
→ plan
→ progress
→ history
→ review
→ memory
```

新的流程建议：

```text
1. git status
2. read current status
3. implement
4. test
5. update current status if meaningful
```

只有：

```text
重大架构决策 → ADR
医学/安全行为变化 → Risk + ADR + Evaluation
重大事故 → Incident
```

才创建额外文档。

---

# 58. 不要重新制造文档膨胀

特别注意：

# 你正在执行“文档精简”。

因此本轮不要一边删除 100 个旧记录，一边创建 40 个新记录。

本轮最多新增：

```text
FINAL_REFACTOR.md
REFACTOR_CHANGELOG.md
POST_REFACTOR_BACKLOG.md
```

如果现有文件可以承载：

甚至不需要三个全新文件。

---

# 59. POST_REFACTOR_BACKLOG

所有本轮明确不做的事情统一放进：

```text
docs/POST_REFACTOR_BACKLOG.md
```

按：

```text
Safety
Medical
Data
Product
Frontend
Infrastructure
```

分类。

只记录任务。

不要现在实现。

---

# 60. 清理 README

最终 README 应该准确描述：

```text
当前正式前端
当前正式 API
当前启动方式
当前目录结构
当前测试方式
安全免责声明
```

不要继续写：

```text
legacy 默认入口
parallel frontend
migration pending frontend
```

如果它们已经被删除。

---

# 61. 更新 Current Architecture

最终：

```text
docs/architecture/current-state.md
```

必须画出真实最终结构。

例如：

```text
Browser
  ↓
React Frontend
  ↓
/api/v1
  ↓
Flask API Adapters
  ↓
Application Services
  ↓
Domain
  ↓
Infrastructure
  ↓
Data / Model
```

不再把旧结构写在第一页。

---

# 62. app.py 最终目标

不设机械行数目标。

但是最终审查：

如果 `app.py` 仍然有：

```text
2000+
```

行：

必须解释为什么。

一般情况下，完成重构后：

它应该明显缩小。

真正业务逻辑应该已经有明确 owner。

---

# 63. 最终目录应该让人一眼看懂

理想：

```text
backend/
  app/
    api/
    application/
    domain/
    infrastructure/

frontend/
  src/

data/
evaluation/
tests/
scripts/

docs/
```

而不是：

```text
新架构
+
旧架构
+
兼容架构
+
旧 UI
+
过渡 UI
+
三套工作流记录
```

---

# 64. 删除文件的安全流程

任何批量删除前：

1. `git grep`
2. 判断 runtime 引用
3. 判断 tests 引用
4. 判断 scripts 引用
5. 判断 CI 引用
6. 判断 docs 引用
7. 删除
8. build
9. tests
10. smoke

不能：

```bash
rm -rf
```

之后再看发生了什么。

---

# 65. 不要删除 Git 历史价值

旧代码已经存在 Git 历史。

所以：

```text
_backup
_old
_legacy
_copy
_tmp
```

文件如果不再参与运行：

通常不需要继续保留。

Git 本身就是版本历史。

---

# 66. 不要提交 build/cache 垃圾

确保 `.gitignore` 覆盖：

```text
frontend/node_modules
frontend/dist
coverage
Playwright output
screenshots 临时文件
pytest cache
Python cache
logs
local JSONL feedback
```

除非某个 baseline 明确需要版本管理。

---

# 67. Dependency Audit

重构结束后检查：

Python：

```text
requirements.txt
requirements-dev.txt
requirements-data.txt
```

Node：

```text
frontend/package.json
```

删除没有 import/use 的依赖。

不要留下迁移过程中加了但最终没使用的库。

---

# 68. Script Audit

检查：

```text
scripts/
parse_hospitals.py
```

分类：

```text
runtime
data build
maintenance
obsolete
```

obsolete 删除。

保留脚本：

README 简单说明用途。

---

# 69. 最终测试层次

## Python

```bash
python -m py_compile ...
python -m pytest
```

---

## Safety

```bash
python -m evaluation.safety.evaluate_safety
```

必须与重构前一致。

---

## Data

```bash
python -m data_validation.validate_datasets ...
```

不要自动修 187 个问题。

---

## Frontend

```bash
npm ci
npm run typecheck
npm run test
npm run build
```

---

## E2E

如果已经加入：

```bash
npm run test:e2e
```

---

# 70. 全量 API Smoke

至少覆盖正式保留的：

```text
health
ready
regions

summary

triage
followups
recommendations

hospitals
hospital detail

doctors
doctor detail

map
evidence
```

检查：

```text
200
400
404
model unavailable
empty result
```

---

# 71. 旧 API 删除后的验证

删除 legacy route 以后：

运行：

```bash
git grep '"/api/'
git grep "'/api/"
```

确保新 frontend 没有错误调用旧接口。

保留的特殊旧接口必须有明确说明。

---

# 72. Dead Reference Audit

最终运行：

```text
git grep legacy
git grep migration_pending
git grep TODO
git grep FIXME
git grep window._
```

逐条判断。

不是要求全部归零。

但每个剩余项必须是有意保留。

---

# 73. 资源文件审计

检查：

```text
duplicate images
backup images
unused favicon
unused Leaflet
unused logos
unused generated assets
```

删除无引用资源。

这一步可以明显减少仓库体积。

---

# 74. 最终工作流必须简单

最终下一个 Agent 开始工作时：

只需要优先看：

```text
README.md
AGENTS.md
docs/status/current.md
docs/architecture/current-state.md
```

而不是先读取：

```text
100+ 个过程记录。
```

这是本轮非常重要的验收目标。

---

# 75. 重构完成定义

只有以下全部满足，才允许宣布：

# REFACTOR COMPLETE

---

## Architecture

* React 前端成为正式默认前端。
* 正式前端只调用当前 canonical API。
* HTTP/Application/Domain/Infrastructure 边界清晰。
* `app.py` 不再承载主要业务实现。
* 不再存在无意义的双实现。

---

## Behavior

* Routine 行为一致。
* Insufficient 行为一致。
* Emergency 行为一致。
* 推荐快照无意外变化。
* Safety 指标无意外变化。

---

## Frontend

* 正式 build 通过。
* 核心 route 刷新正常。
* desktop/mobile smoke 通过。
* 无关键 console error。
* 无关键 404。

---

## Backend

* pytest 通过。
* API smoke 通过。
* model unavailable 退化正常。
* data validation 正常执行。

---

## Legacy

* legacy 前端已删除，或者存在非常明确且必要的保留理由。
* dead legacy route 已删除。
* dead wrappers 已删除。
* dead assets 已删除。
* backup 文件已删除。

---

## Repository

* 无明显重复代码。
* 无明显重复依赖。
* 无临时 build/cache。
* 无不必要二进制。
* 目录职责清晰。

---

## Documentation

* Current Status 是事实。
* Architecture 是事实。
* README 是事实。
* 工作流大幅精简。
* 过程性 plans/progress/history 已合并或删除。
* memory 精简。
* ui-registry 精简。
* 不再要求每个小 Slice 生成多份记录。

---

# 76. 最终完成以后再做一次 Review

按以下顺序：

### 1. Architecture Review

有没有仍然存在：

```text
旧新两套 owner
循环依赖
legacy fallback
重复实现
```

---

### 2. Runtime Review

从：

```bash
python app.py
```

开始。

模拟一个第一次拿到项目的人。

检查是否能够正常运行。

---

### 3. Repository Review

问：

> 如果今天这个仓库第一次公开，我会不会觉得这里有大量重构垃圾？

如果会：

继续清理。

---

### 4. Documentation Review

问：

> 下一位 Agent 是否能在 5 分钟内知道项目现在是什么状态？

如果不能：

继续精简。

---

# 77. 最后输出一份非常简洁的完成报告

不要再写超长 Progress 文档。

最终回复用户：

```text
Refactor Complete

1. Backend
- ...

2. Frontend
- ...

3. Removed
- ...

4. Documentation cleanup
- ...

5. Tests
- ...

6. Safety baseline
- ...

7. Remaining backlog
- ...
```

列出删除的重要文件 / 目录。

列出：

```text
app.py before / after
legacy routes before / after
docs records before / after
repository size before / after
```

如果方便测量。

---

# 78. Git 策略

不要：

```text
git reset --hard
force push
覆盖用户改动
```

可以按逻辑步骤提交。

例如：

```text
refactor: complete backend application boundaries

refactor: cut over competition frontend

refactor: retire legacy frontend and routes

chore: remove unused assets and dependencies

docs: consolidate refactor records and simplify workflow
```

除非用户明确授权：

不要 push。

---

# 79. 本轮结束之后才允许进入功能开发

只有出现：

```text
REFACTOR COMPLETE
```

并通过最终验证之后：

才开始读取：

```text
POST_REFACTOR_BACKLOG.md
```

讨论新功能。

在此之前：

# 不加功能。

---

# 80. 现在开始

现在请按下面的顺序直接执行，不要先问用户大量问题：

```text
1. 恢复最新上下文
2. 检查 git status / HEAD
3. 对齐 handoff 与真实代码
4. 建立 FINAL_REFACTOR 单一计划
5. 完成 backend 剩余边界
6. 完成 legacy route parity
7. 验证 React frontend
8. 切换 React 为正式 /
9. 运行核心 E2E / smoke
10. 删除 legacy frontend
11. 删除 dead legacy route / wrapper
12. 删除无用 asset / backup / binary
13. 删除无用 dependency / script
14. 合并并删除重复 workflow records
15. 精简 AGENTS / memory / ui-registry
16. 更新 README / status / architecture
17. 全量测试
18. 最终 review
19. 输出 REFACTOR COMPLETE 报告
```

最重要的约束再次强调：

> **这轮的价值不在“做更多”，而在“彻底完成”。**

> **不要新增功能。不要优化医学算法。不要扩大产品范围。**

> **把现在同时存在的新架构、旧架构、兼容层、迁移记录真正收敛成一套正式架构。**
