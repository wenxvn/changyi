你正在继续维护 GitHub 仓库：

`https://github.com/wenxvn/changyi`

当前架构重构已经完成。

不要再进行大规模架构重构。

当前正式架构已经是：

```text
React / TypeScript Frontend
        ↓
      /api/v1
        ↓
Flask HTTP Adapter
        ↓
Application Services
        ↓
Domain
        ↓
Infrastructure
```

当前目标已经从：

```text
Architecture Refactor
```

切换为：

# P0 Product Hardening

即：

> 修复重构后出现的功能回归、医疗安全问题、产品文案污染、核心页面布局问题，并建立真正的浏览器级产品验收能力。

---

# 0. 本轮总原则

本轮不是：

```text
继续拆后端
继续换框架
继续加页面
继续增加新能力
```

而是：

```text
恢复已有能力
↓
修复 P0 Safety
↓
清理产品文案
↓
修复核心 UI
↓
建立浏览器验收
↓
达到稳定产品基线
```

本轮只做以下五个 P0：

```text
P0-1 Safety 漏检修复

P0-2 恢复高德地图 / 到院导航能力

P0-3 清理提示词 / 工程语境污染

P0-4 修复核心页面布局与明显视觉问题

P0-5 建立 Playwright + Visual QA
```

除此之外：

# 不加新产品功能。

---

# 1. 开始前必须恢复上下文

首先读取：

```text
AGENTS.md
README.md
memory.md

docs/status/current.md
docs/architecture/current-state.md
docs/risks/register.md
docs/quality/quality-gates.md

docs/POST_REFACTOR_BACKLOG.md
docs/REFACTOR_CHANGELOG.md

ui-registry.md
```

然后读取：

```text
frontend/
backend/
tests/
evaluation/
data_validation/
.github/workflows/
```

特别检查：

```text
frontend/src/pages/
frontend/src/components/
frontend/src/styles/
frontend/src/api/
frontend/src/state/

backend/app/domain/triage/
backend/app/application/
backend/app/api/v1/
```

开始前：

```bash
git status --short --branch
git log --oneline -10
git diff
```

不要覆盖用户已有修改。

---

# 2. 当前基线

当前正式提交基线应核实是否仍为：

```text
08a4896
Consolidate application around the current Flask and React architecture
```

当前已知：

```text
app.py 已成为薄启动层
React 已成为正式前端
旧模板 / static/js/app.js / static/css/style.css 已删除
/api/v1 为正式 API
Legacy route 已基本删除
CI 已成功运行
107 pytest 通过
Frontend typecheck / test / build 通过
```

因此：

# 不要重新做架构重构。

---

# 3. 一个非常重要的原则：重构不能丢功能

上一轮出现了一个明确的 Regression：

重构前：

```text
医院 → 立即导航 → 高德地图
```

存在。

重构后：

```text
MapPage → 位置示意 · 非导航地图
```

导航能力被删除。

原因是：

上一轮把：

```text
“不在重构阶段新增导航”
```

错误理解成：

```text
“导航能力可以不迁移”
```

以后禁止这种行为。

原则：

> 已经存在且仍有产品价值的能力，在重构中默认必须保留。

如果决定删除：

必须说明：

```text
为什么删除
谁确认删除
替代能力是什么
```

---

# 4. P0-1 — Safety 漏检修复

这是整个项目当前优先级最高的问题。

当前 Safety Evaluation：

```text
Cases: 16

Red Flag Recall:
0.9231

Under-triage:
0.0769

Over-triage:
0.0

Emergency False Negative:
1
```

这是已知风险。

当前 backlog 还明确记录了：

```text
口语呼吸困难
压榨样胸痛
胸痛 + 呼吸困难 + 冷汗
模糊输入
信息不足
```

存在需要人工审核的边界。

---

# 5. Safety 本轮允许修改医学行为

注意：

上一轮重构冻结医学规则。

本轮不再冻结。

因为：

# 这轮就是专门修 P0 Safety。

但是所有医学行为变化必须按 L3 执行。

流程：

```text
建立 case
↓
确认当前错误行为
↓
记录 ADR / Safety change
↓
修改 rule
↓
跑 Safety Evaluation
↓
跑 regression
↓
确认没有破坏其他 case
```

---

# 6. Safety 首先修复 Emergency False Negative

优先检查：

```text
胸口压榨样疼痛
喘不过气
明显出汗 / 冷汗
```

这种组合。

目标：

如果文本明显出现：

```text
典型危险胸痛
+
呼吸困难
+
自主神经症状
```

Safety Gate 应优先进入：

```text
EMERGENCY
```

而不是：

```text
ROUTINE
↓
Follow-up
```

---

# 7. 不要简单 keyword patch

不要：

```python
if "胸痛" in text:
    emergency = True
```

这种粗暴规则。

应该检查现有：

```text
normalization
negation
entities
red flag categories
combination rules
```

尽量在现有 Safety domain 中修。

需要考虑：

```text
否定：
“没有胸痛”

历史描述：
“以前胸痛，现在没有”

疑问：
“胸痛是不是心脏病”

轻微非危险表达
```

避免 Over-triage 明显升高。

---

# 8. Safety Evaluation 扩充

当前只有：

```text
16 cases
```

太少。

本轮可以增加 Safety test cases。

但不是为了刷指标。

建议新增：

```text
胸痛组合
呼吸困难组合
脑卒中组合
过敏组合
急腹症组合
意识异常
严重外伤
妊娠危险
眼科急症
精神危机
否定词
模糊口语
信息不足
```

至少形成：

```text
30–50 个
```

经过明确标注的 Safety regression cases。

---

# 9. Safety 的核心指标

至少输出：

```text
Red Flag Recall
Emergency False Negative
Under-triage Rate
Over-triage Rate

Emergency case count
Urgent case count
Routine case count
Insufficient case count
```

优先级：

```text
Emergency False Negative
>
Red Flag Recall
>
Under-triage
>
Over-triage
```

---

# 10. 本轮 Safety 不做诊断模型升级

不要：

```text
换 Transformer
接 LLM
重训大模型
增加疾病类别
```

只修：

```text
Safety Gate
rule composition
medical input interpretation
```

---

# 11. P0-2 — 恢复高德导航

这是：

# Regression restoration

不是新功能。

---

# 12. 先阅读 Git 历史中的旧实现

恢复前必须查看旧提交：

```text
ba74fd0
35b4f3d
```

中的：

```text
static/js/app.js
```

重点搜索：

```text
openHospitalNavigation
uri.amap.com
gaodeTile
autonavi
locateUser
Leaflet
```

理解旧功能后再实现。

不要凭记忆重新造一套。

---

# 13. 旧版已经存在的能力

旧版至少存在：

```text
openHospitalNavigation(hid)
```

并使用：

```text
https://uri.amap.com/navigation
```

如果有经纬度：

```text
to=lng,lat,hospital_name
```

没有经纬度：

回退：

```text
https://www.amap.com/search
```

这是一个非常成熟且低风险的逻辑。

优先恢复。

---

# 14. 第一阶段只恢复外部高德导航

不要一上来重新做复杂地图 SDK。

优先实现：

```text
医院卡片
      ↓
去这里 / 高德导航

医院详情
      ↓
高德导航

推荐结果医院
      ↓
高德导航

Map Resource Preview
      ↓
高德导航
```

按钮统一文案：

```text
高德导航
```

或：

```text
去这里
```

点击之后：

打开：

```text
uri.amap.com/navigation
```

---

# 15. 抽成统一 utility

不要每个页面重复 URL 逻辑。

创建类似：

```text
frontend/src/utils/navigation.ts
```

包含：

```ts
buildAmapNavigationUrl()
openAmapNavigation()
```

输入：

```text
name
address
lat
lng
```

---

# 16. Navigation fallback

如果有：

```text
lat
lng
```

使用：

```text
高德 navigation URI
```

否则：

```text
高德 search
```

如果：

```text
name
address
```

都缺：

按钮 disabled。

---

# 17. 不要泄露 API Key

当前 Git 历史中未找到正式高德 JS SDK key。

因此第一阶段不要添加：

```text
硬编码高德 API Key
```

如果未来需要 JS SDK：

使用：

```text
environment variable
```

不要提交 key。

---

# 18. 地图页面本轮可以升级，但有限

当前 `/map` 本质是：

```text
坐标示意图
```

并不是真实地图。

本轮最低要求：

```text
Resource location
+
Hospital selection
+
External navigation
```

如果现有架构下可以低风险接入真实地图：

可以做。

但必须满足：

```text
不暴露 secret
不破坏 CI
不依赖付费服务
不影响核心页面加载
```

否则先只恢复高德外部导航。

---

# 19. 不要再写“非导航地图”

导航恢复后：

删除明显冲突的文案：

```text
位置示意 · 非导航地图
不替代导航
```

可以改为：

```text
医院位置分布
```

以及：

```text
距离为直线距离，仅供参考。
实际路线请以高德地图导航结果为准。
```

这个表达更合理。

---

# 20. 用户定位

旧前端曾支持：

```text
navigator.geolocation
```

本轮可恢复最基础定位：

```text
用户主动点击“定位”
```

不要：

```text
页面一打开就弹权限
```

定位用途：

```text
计算直线距离
地图初始视角
```

不能宣称：

```text
实时导航距离
实时到院时间
```

---

# 21. P0-3 — 清理提示词 / 工程语境污染

这是前端当前非常明显的问题。

当前 UI 中存在大量：

```text
Agent 实现时写给开发者看的语言
```

不应该直接给普通用户看。

---

# 22. 普通页面禁止出现以下词

除非它属于“可信 AI → 技术详情”。

禁止：

```text
评委
比赛
竞赛
参赛
获奖
赛道

prompt
提示词
Agent

v1
API
endpoint

backend
后端安全门

migration
迁移中
migration_pending

provenance

repo
仓库

rollback
回滚

runtime
当前运行

schema

SHA-256
```

---

# 23. 建立 Product Copy Audit

扫描：

```text
frontend/src
frontend/dist
```

搜索：

```text
评委
比赛
竞赛
参赛
v1
后端
迁移
provenance
仓库
回滚
API
schema
```

逐条人工判断。

---

# 24. 文案改写原则

产品 UI 应该解释：

```text
用户发生了什么
用户下一步做什么
系统为什么这样做
```

不要解释：

```text
代码怎么实现
后端怎么调用
API 怎么设计
迁移做到哪
```

---

# 25. 已知需要改的文案

例如：

现在：

```text
当前状态由后端安全门返回
```

改为：

```text
当前未发现需要立即急诊处理的危险信号。
```

---

现在：

```text
资源推荐会单独从 v1 获取
```

改为：

```text
继续查看当前就医资源建议。
```

---

现在：

```text
已从 v1 读取
```

直接删除。

---

现在：

```text
医院目录逐字段 provenance 仍在迁移中
```

改为：

```text
部分医院资料的来源信息仍在补充核验。
```

---

现在：

```text
首页指标来自当前运行数据。数据来源与迁移状态同步展示。
```

改为：

```text
以下数据来自当前已接入的常州医疗与交通资源。
```

---

现在：

```text
来源迁移中
```

改为：

```text
来源待核验
```

或：

```text
资料核验中
```

---

现在：

```text
所有指标均来自当前代码仓库中的离线评测与数据报告
```

改为：

```text
以下指标来自系统当前离线评估结果，仅用于说明模型与安全边界。
```

---

现在：

```text
知道当前运行的，才能知道如何回滚。
```

改为：

```text
每一次判断，都应该可以追溯到明确的版本。
```

---

# 26. Trust Center 分两层

当前 Trust 页面过于开发者化。

改成：

## 默认用户层

展示：

```text
安全评估
模型表现
数据来源
系统限制
```

用户应该能看懂。

---

## 技术详情

可以折叠：

```text
模型版本
规则版本
数据版本
SHA-256
API / schema
```

技术内容不能抢占默认页面。

---

# 27. 页面文案必须完全脱离“比赛”

即使项目是参赛作品：

网页本身不能写：

```text
为了评委
方便评委
比赛展示
竞赛版本
参赛作品
```

产品应该像：

> 真正准备给用户使用的产品。

比赛是外部场景。

不是产品身份。

---

# 28. 增加 forbidden-copy test

增加一个简单静态测试。

例如扫描：

```text
frontend/src
```

如果普通产品页面出现：

```text
评委
比赛
竞赛
参赛
```

CI 直接失败。

对于：

```text
v1
migration
backend
provenance
```

可以采用 allowlist：

只允许：

```text
Trust technical detail
```

---

# 29. P0-4 — 核心页面布局修复

这一阶段不是重新设计整个网站。

现有：

```text
Warm Precision
Humanist Future Healthcare
```

视觉语言继续保留。

本轮只解决：

```text
明显难看
明显不平衡
明显空白
阅读路径不自然
```

的问题。

---

# 30. 优先级最高：Triage 页面

当前：

```text
左边：
标题
说明

右边：
输入
理解
追问
结果
医院
医生
解释
```

结果：

页面向下滚动后：

```text
左边完全空白
右边非常长
```

必须修复。

---

# 31. Triage 推荐状态转换

不要整个生命周期永远使用一个双栏。

采用：

# State-based Layout

---

## State A — 初始状态

可以保持：

```text
40 / 60 双栏
```

左边：

```text
标题
解释
Safety note
```

右边：

```text
症状输入
```

---

## State B — 分析开始后

布局转换为：

```text
Context Sidebar
+
Main Workflow
```

例如：

```text
┌──────────────┬──────────────────────────┐
│ 当前理解       │ 追问 / 结果               │
│              │                          │
│ 症状          │ Safety                  │
│ 时间          │                          │
│ 伴随症状       │ 科室                     │
│              │                          │
│ 风险          │ 医院                     │
│              │                          │
│ 推荐方向       │ 医生                     │
│              │                          │
│              │ 推荐解释                  │
└──────────────┴──────────────────────────┘
```

左侧可以：

```text
position: sticky
```

但重点不是 sticky。

重点是：

> 左侧内容必须变成有持续价值的 Context。

---

# 32. Current Understanding 应成为左侧主角

当前：

```text
CurrentUnderstanding
```

已经存在。

不要重新做数据逻辑。

把它提升为：

```text
persistent contextual sidebar
```

包含：

```text
主要症状
持续时间
伴随症状
风险
分诊状态
推荐科室
```

---

# 33. 不要让输入框永远占据顶部

用户提交以后：

输入区域可以折叠成：

```text
你的描述
“……”
[修改]
```

这样后面结果不会被巨大 textarea 占空间。

---

# 34. Results 页面层级

结果顺序应该严格为：

```text
Safety Status
↓
下一步行动
↓
科室
↓
首选医院路径
↓
备选医院
↓
医生
↓
为什么推荐
↓
来源
```

不是：

```text
技术状态
↓
各种卡片
↓
推荐结果
```

---

# 35. Resources 页面

当前：

```text
左侧多张卡
右侧 sticky detail
```

容易出现：

```text
左右高度巨大不平衡
```

需要检查 desktop 实际视觉。

可以考虑：

```text
列表 + Drawer / Modal Detail
```

或者：

```text
master-detail
```

但 detail 不应让整页产生视觉断裂。

---

# 36. 推荐 Resources 最终交互

Desktop：

```text
Resource grid/list
```

点击之后：

```text
Right Drawer
```

或者：

```text
Detail overlay
```

这样不会出现：

```text
右边只有一个详情
左边几十项
```

导致布局失衡。

如果现有 sticky detail 已经足够合理：

可以优化。

不强制重写。

---

# 37. Map 页面

地图如果保留双栏：

```text
左：
资源列表

右：
地图
```

则：

右地图应：

```text
sticky
```

左边允许滚动。

不要：

```text
列表只有几项
地图超高

或

列表几十项
地图很快结束
```

---

# 38. Trust 页面

Trust 内容很多。

不要全部横向两栏。

推荐：

```text
Header
↓
Safety
↓
Model
↓
Data
↓
Limitations
```

每个 section 可以单独布局。

避免：

```text
左边 1 个 card
右边 4 个 card
```

造成视觉重量失衡。

---

# 39. Profile 页面

Profile 本轮不是重点。

只检查：

```text
空白是否过大
信息量是否过少
是否太像内部 Demo 页面
```

例如：

```text
LOCAL DEMO
常州演示访客
本地演示
```

如果视觉上明显像：

```text
开发测试页
```

需要产品化。

可以改成：

```text
本地偏好
最近分析
```

不需要强调：

```text
DEMO
```

---

# 40. 删除不必要的 Hero 重复模式

当前多个页面都可能使用：

```text
大标题
右上角一个很小 note
下面长内容
```

这会造成模板感。

逐页检查：

```text
Resources
Map
Trust
Profile
```

不要全部长得完全一样。

统一品牌 ≠ 完全同模板。

---

# 41. Layout Audit

针对：

```text
/
 /triage
 /resources
 /map
 /trust
 /profile
```

在：

```text
1440×900
1280×800
1024×768
768×1024
390×844
```

检查：

```text
左右高度
空白
视觉中心
标题长度
sticky
footer
滚动节奏
```

---

# 42. 不要为了“填空白”堆 Card

发现左边空白：

不要：

```text
随便加 3 张信息卡
```

正确做法是：

```text
重新调整信息架构
```

---

# 43. P0-5 — Playwright + Visual QA

当前 CI 最大问题：

```text
编译通过
```

但是：

```text
不知道页面是否好用
```

必须补真正的浏览器测试。

---

# 44. 使用 Playwright

加入：

```text
@playwright/test
```

不要使用大型复杂 E2E 框架。

---

# 45. E2E 必须跑真实 Flask + build

不是只跑：

```text
Vite mock
```

理想流程：

```text
npm run build

python app.py

Playwright
→ http://127.0.0.1:5002
```

确保正式 demo 路径真的可用。

---

# 46. 最低 E2E

必须包含：

## Home

```text
页面加载
主要 CTA 可点击
进入 Triage
```

---

## Routine

输入：

```text
最近皮肤一直很痒，大概一周，没有呼吸困难，也没有发烧
```

确认：

```text
不是 Emergency
出现就医方向
可以继续资源路径
```

---

## Insufficient

输入：

```text
最近总是头晕
```

确认：

```text
需要 Follow-up
```

---

## Emergency

输入：

```text
胸口压榨样疼痛，喘不过气，还一直冒冷汗
```

确认：

```text
EMERGENCY
不出现普通推荐
出现 120
```

注意：

P0-1 修完后：

这个用例必须通过。

---

# 47. Navigation E2E

选择一家有坐标医院。

确认：

```text
高德导航按钮存在
```

URL：

```text
uri.amap.com/navigation
```

且包含：

```text
lng
lat
name
```

不能真的跳出测试浏览器访问第三方。

只断言 URL 正确。

---

# 48. Page Smoke

覆盖：

```text
/resources
/map
/trust
/profile
```

至少确认：

```text
200
关键标题
无错误态
```

---

# 49. Browser console

Playwright 中：

如果出现：

```text
console.error
pageerror
```

测试失败。

除非明确 allowlist。

---

# 50. Network

检查：

```text
404
500
```

不应该在核心页面加载时出现。

---

# 51. Visual Screenshots

至少保存：

```text
home-desktop
home-mobile

triage-initial
triage-followup
triage-routine
triage-emergency

resources
map
trust
```

---

# 52. Visual Regression

如果仓库适合保存 baseline：

使用：

```text
Playwright screenshot comparison
```

否则：

至少在 artifact 上传 screenshots。

CI 不一定因为像素差异失败。

但必须可查看。

---

# 53. Screenshot 不能代替人工 QA

Agent 每完成一个页面：

实际打开截图。

检查：

```text
视觉平衡
空白
行宽
层级
button
sticky
footer
```

不能：

```text
只写 CSS
↓
npm build
↓
宣布完成
```

---

# 54. Responsive 最低要求

Desktop：

```text
1440×900
1280×800
```

Tablet：

```text
768×1024
```

Mobile：

```text
390×844
```

---

# 55. Accessibility

继续保持：

```text
keyboard
focus-visible
aria
reduced-motion
semantic HTML
```

新增页面修改不能破坏。

---

# 56. Product Copy Test

CI 新增静态测试：

普通页面不能出现：

```text
评委
比赛
竞赛
参赛
```

建议同时警告：

```text
v1
migration
后端
provenance
```

---

# 57. 不要测试 implementation detail

E2E 测试用户行为。

例如：

好：

```text
输入症状
点击分析
出现 Safety 状态
```

差：

```text
div:nth-child(4)
```

尽量使用：

```text
role
label
text
test id
```

---

# 58. 前端设计系统不重做

继续使用：

```text
frontend/src/styles/tokens.css
ui-registry.md
```

保持：

```text
Warm Porcelain
Deep Ink
Indigo
Medical Teal
Sage
Clinical Red
Amber
```

本轮不是重新选色。

---

# 59. 允许做的视觉优化

允许：

```text
spacing
grid
column proportion
sticky behavior
section rhythm
typography size
line-height
button hierarchy
card density
mobile spacing
```

---

# 60. 禁止的视觉大改

不要：

```text
换整套颜色
换品牌
换 logo
改首页完整视觉方向
加入 Three.js
重做所有动画
```

这些不是本轮目标。

---

# 61. 首页只做文案与 QA

首页整体方向已经基本成立。

只检查：

```text
开发文案
migration 文案
非用户语言
responsive
CTA
```

不重做 Hero。

---

# 62. 恢复“就医闭环”

本轮产品体验目标：

```text
用户描述症状
↓
Safety
↓
科室
↓
医院
↓
医生
↓
高德导航
```

这才是真正完整路径。

---

# 63. 医院导航按钮位置

至少存在于：

```text
Recommendation hospital card

Hospital detail

Resource hospital detail

Map preview
```

不要只在 `/map` 有。

---

# 64. 医生页面不直接导航医生

医生属于医院。

医生详情可以：

```text
查看所在医院
↓
导航到医院
```

不要直接假设医生独立地址。

---

# 65. External link UX

打开高德：

```text
target="_blank"
rel="noopener noreferrer"
```

按钮旁可使用：

```text
ExternalLink
Navigation
```

icon。

---

# 66. Emergency 导航要谨慎

Emergency 页面优先：

```text
120
```

其次：

```text
附近急诊
```

如果增加高德导航：

必须放在：

```text
拨打 120
```

之后。

不要让：

```text
自行导航医院
```

视觉优先级高于：

```text
紧急呼叫
```

---

# 67. Safety UI 修完规则后同步

如果 Safety Gate 变更：

Emergency 页面必须确认：

```text
疾病候选隐藏
普通医生推荐隐藏
普通 follow-up 隐藏
```

保持 Safety-first publication。

---

# 68. 数据 / provenance 本轮只改文案

不要趁本轮：

```text
重新收医院数据
重新爬医生数据
修改 187 个异常
```

这些仍然是后续任务。

本轮只确保：

```text
不向普通用户暴露工程字段
```

---

# 69. 不要把 187 个异常自动修掉

数据质量问题继续保留。

如果视觉页面需要：

```text
数据核验中
```

即可。

不要静默修改正式 data。

---

# 70. 不要增加登录

Profile 不升级。

不做：

```text
OAuth
账号
数据库
云历史
```

---

# 71. 不要增加 LLM / Agent

当前系统不是 ChatGPT wrapper。

保持。

不接：

```text
OpenAI
DeepSeek
Agent
RAG
```

---

# 72. 测试环境不得使用真实患者信息

所有 E2E 输入：

使用固定测试文本。

不要：

```text
姓名
联系方式
真实病历
```

---

# 73. CI 最终目标

现有：

```text
Python 3.11
Python 3.12

pytest
Safety
data validation
characterization

frontend typecheck
frontend tests
frontend build
```

新增：

```text
Playwright smoke
Product copy audit
```

不要建立第二套 workflow。

直接扩展：

```text
.github/workflows/quality.yml
```

---

# 74. Safety Metrics 必须写入 CI Log

CI 输出：

```text
Emergency False Negative
Red Flag Recall
Under-triage
Over-triage
```

方便快速看 regression。

---

# 75. Safety 门禁建议

本轮修复后：

最低要求：

```text
Emergency False Negative = 0
```

针对当前 Safety Set。

并尽量：

```text
Red Flag Recall = 1.0
```

如果无法做到：

不得伪造。

明确报告剩余 case。

---

# 76. 不要追求 100% 指标而破坏规则

如果把所有输入都判 Emergency：

指标会很好看。

但产品错误。

必须同时检查：

```text
Over-triage
```

---

# 77. 更新 Safety Card

Safety 修复后：

更新：

```text
docs/competition/2026-ai-medical/SAFETY_CARD.md
```

或当前 equivalent。

记录：

```text
修复前
修复后
case
原因
限制
```

---

# 78. Architecture 不需要再改

除非为了：

```text
导航 utility
Playwright
```

这种小边界。

不要再拆：

```text
application service
domain
repository
```

---

# 79. docs 本轮保持精简

不要重新开始：

```text
plan
progress
history
review
```

四份一套。

本轮最多新建：

```text
docs/plans/P0_PRODUCT_HARDENING.md
```

一份。

其他更新：

```text
docs/status/current.md
docs/risks/register.md
docs/POST_REFACTOR_BACKLOG.md
```

即可。

---

# 80. Product Hardening Plan

唯一计划：

```text
docs/plans/P0_PRODUCT_HARDENING.md
```

包含：

```text
P0-1 Safety
P0-2 Navigation
P0-3 Copy
P0-4 Layout
P0-5 E2E
```

每项：

```text
problem
scope
acceptance
rollback
status
```

---

# 81. 本轮不要删除旧 Git 历史

不要：

```text
squash history
force push
rewrite history
```

旧高德代码通过 Git 查看即可。

不要重新恢复：

```text
static/js/app.js
```

整个文件。

只迁移需要的能力。

---

# 82. 不要恢复旧视觉

旧高德逻辑有价值。

旧后台 UI 没有必要恢复。

原则：

```text
功能恢复
≠
UI 回滚
```

---

# 83. Map 技术方向

短期：

```text
React resource map visualization
+
AMap URI navigation
```

中期以后再评估：

```text
AMap JS API
```

不要本轮强行做复杂 SDK。

---

# 84. URL Builder 测试

为：

```text
buildAmapNavigationUrl
```

写 unit tests。

Cases：

```text
valid lat/lng
missing lat/lng
name
address
special Chinese chars
```

---

# 85. 产品文案语言

中文优先。

英文只作为：

```text
eyebrow
section label
```

不要：

```text
一个页面 50% 英文
```

---

# 86. “示范区”可以保留

例如：

```text
常州示范区
Changzhou Pilot
```

是合理产品信息。

但：

```text
比赛示范
参赛示范
评委体验
```

禁止。

---

# 87. “原型”使用要克制

Trust 页面可以说明：

```text
当前系统仍处于研究与演示阶段。
```

但不要每个页面：

```text
Demo
Prototype
Migration
```

不断提醒。

这样会破坏产品感。

---

# 88. 前端完成后做一次 Copy Review

假设：

> 你完全不知道这是参赛项目。

阅读页面。

如果出现一句话让你觉得：

```text
“为什么产品要跟我说这个？”
```

那就改。

---

# 89. 前端完成后做一次 Visual Review

逐页回答：

```text
视线先看到什么？
下一步操作是否清晰？
左右重量是否平衡？
页面下面有没有大片死区？
信息是否太密？
有没有模板感？
```

---

# 90. Triage 最重要

如果只能精修一个页面：

# Triage 优先。

因为它是整个产品的核心。

目标：

打开：

```text
高级
简洁
可信
```

输入之后：

```text
AI-native
结构化
有过程
```

结果：

```text
明确下一步
```

---

# 91. Emergency 是第二重要

输入危险症状后：

页面应该明显进入：

```text
Emergency state
```

不能：

```text
普通页面 + 红 badge
```

保持之前已经建立的独立 Emergency UI。

只确保 Safety 修复后能够正确触发。

---

# 92. Map 是第三重要

原因：

```text
AI 推荐
↓
真实城市资源
↓
真实到院路径
```

是这个项目区别于普通疾病预测 Demo 的关键。

---

# 93. 不能再出现功能回退

本轮修改任何页面前：

检查：

```text
当前有哪些 action
```

修改后：

必须逐个验证。

---

# 94. Regression Checklist

每个核心页面：

```text
Before
After
```

记录：

```text
buttons
flows
API
states
navigation
```

避免：

```text
UI 更漂亮
功能少了一半
```

---

# 95. 最终验收标准

## Safety

```text
Emergency False Negative = 0
```

针对当前正式 Safety Set。

没有新严重 regression。

---

## Navigation

```text
推荐医院 → 高德导航
医院详情 → 高德导航
Map Preview → 高德导航
```

全部可用。

---

## Copy

普通页面不存在：

```text
评委
比赛
竞赛
v1
migration
后端安全门
provenance
```

等明显工程污染。

---

## Layout

至少：

```text
/triage
/resources
/map
/trust
```

不存在明显：

```text
左空右长
巨大死区
异常 sticky
视觉失衡
```

---

## Browser

Playwright 核心流程通过。

---

## CI

全部 green。

---

# 96. 最终浏览器矩阵

必须人工 / Agent 浏览：

```text
1440×900
1280×800
768×1024
390×844
```

页面：

```text
/
 /triage
 /resources
 /map
 /trust
 /profile
```

状态：

```text
empty
loading
error
routine
urgent
emergency
followup
```

---

# 97. Screenshot Matrix

最终输出截图：

```text
home
triage-initial
triage-understanding
triage-followup
routine-result
emergency-result
resources
map
trust
mobile-home
mobile-triage
```

---

# 98. 最终 Safety 报告

对比：

```text
BEFORE

Recall 0.9231
Under-triage 0.0769
Emergency FN 1

AFTER

...
```

不要只写：

```text
Safety improved
```

---

# 99. 最终 Navigation 报告

说明：

```text
恢复了哪些旧能力

哪些能力仍未恢复

是否使用 AMap URI

是否使用 API Key

是否请求定位
```

---

# 100. 最终 Copy Audit

输出：

```text
Removed product-facing engineering terms:
- ...
- ...
```

---

# 101. 最终 UI 报告

只列实际修过的页面：

```text
Triage
Resources
Map
Trust
Profile
```

分别说明：

```text
problem
solution
```

---

# 102. 不要宣布“完成”直到真实浏览器验证

必须：

```text
build
↓
serve
↓
open
↓
interact
↓
screenshot
↓
review
```

之后再宣布完成。

---

# 103. 本轮 Git 提交建议

保持小提交。

例如：

```text
fix: close emergency triage safety gaps

fix: restore amap hospital navigation

fix: remove product-facing implementation language

fix: rebalance triage and resource layouts

test: add playwright product smoke coverage
```

不要一个 5000 行提交完成全部。

---

# 104. 不要 push

除非用户明确授权：

不要：

```text
git push
```

可以 commit。

---

# 105. 最终交付格式

最后向用户报告：

```text
P0 Product Hardening Complete

1. Safety
Before:
After:

2. AMap Navigation
Restored:
Remaining:

3. Copy Cleanup
Removed:

4. UI
Changed:

5. E2E
Cases:
Result:

6. CI
Result:

7. Remaining P1
- ...
```

---

# 106. 本轮结束后才进入 P1

P1 才考虑：

```text
医院/医生逐字段 provenance
187 个数据质量问题
更完整真实地图
实时交通
结构化 follow-up API
生产认证
新城市
新功能
```

本轮不要顺带做。

---

# 107. 最重要的最终原则

请持续遵守下面五条：

> **1. Safety 比视觉优先。**

> **2. 重构不能以删除已有产品能力为代价。**

> **3. 产品页面只说用户需要知道的话，不暴露提示词、比赛语境和工程实现。**

> **4. 前端布局要根据内容状态变化，而不是永远套固定双栏模板。**

> **5. 编译通过不是产品验收，真实浏览器体验才是。**

---

现在开始执行。

顺序严格为：

```text
1. Read current baseline
2. Create one P0 hardening plan
3. Reproduce Safety FN
4. Fix Safety
5. Expand Safety regression set
6. Restore AMap navigation
7. Audit all product copy
8. Clean engineering/prompt language
9. Rework Triage layout
10. Fix Resources / Map / Trust imbalance
11. Add Playwright
12. Add screenshots
13. Run full CI
14. Browser QA
15. Update current status
16. Final report
```

不要在中途扩展 scope。
