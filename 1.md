你现在维护 GitHub 仓库：

`https://github.com/wenxvn/changyi`

项目名称：**常医智导**

这是一个面向常州市医疗资源检索、辅助分诊、医院/医生推荐和就医路径规划的 AI+医疗 Web 项目。

本次任务不是新增功能，也不是重新重构项目。

你的任务是：

# 对当前最新版本进行一次真实工程审计，并修复 3 个已经发现的高优先级问题

我会根据你的代码阅读能力、问题判断能力、修改质量、测试质量和是否产生副作用来评价你的表现。

---

# 一、工作方式

先完整读取仓库当前状态。

至少阅读：

```text
README.md
AGENTS.md
memory.md

docs/status/current.md
docs/architecture/current-state.md
docs/risks/register.md
docs/POST_REFACTOR_BACKLOG.md
docs/REFACTOR_CHANGELOG.md

frontend/src/
backend/app/
evaluation/
data_validation/
data/regions/320400/
tests/
.github/workflows/quality.yml
```

然后运行：

```bash
git status
git log --oneline -10
```

确认当前最新提交。

不要假设提示词中的文件一定与当前仓库完全一致，以仓库实际状态为准。

---

# 二、禁止事项

不要：

```text
重新设计整个架构
替换 Flask
替换 React
引入 Next.js
引入数据库
接入 LLM
接入 Agent
新增全国城市
新增账号系统
大规模删除代码
重写医学规则
修改当前 Safety Gate 业务语义
```

不要为了“代码更漂亮”进行无关重构。

这次只修明确问题。

---

# 三、问题 1：地图页面底图当前存在真实显示异常

目前项目已经恢复真实地理地图。

重点检查：

```text
frontend/src/pages/MapPage.tsx
frontend/src/api/map.ts
frontend/src/styles/globals.css
```

当前地图使用的 CARTO / OSM tile 在实际浏览器截图中可能显示：

```text
API KEY REQUIRED
```

即：

* HTTP 请求可能成功；
* Playwright 不一定报错；
* 但用户看到的地图其实已经坏了。

你的任务不是“让测试绿”。

而是：

# 让地图在真实浏览器里正确显示。

---

# 四、地图修复要求

你需要判断当前底图服务为什么出现该问题。

然后采用一个：

```text
稳定
公开
授权清晰
无需把 secret 提交到 Git
适合比赛 Demo
```

的实现。

如果使用需要 API Key 的地图：

必须从环境变量读取。

例如：

```text
VITE_XXX_MAP_KEY
```

禁止把：

```text
API key
security code
token
```

写入源码或提交到仓库。

如果存在无需密钥且适合当前使用场景的地图方案，可以优先使用。

---

# 五、地图降级

地图 tile 加载失败时必须继续安全降级。

至少保留：

```text
医院列表
医院名称
医院坐标
医院详情
高德外部导航
```

不要因为地图第三方服务异常导致整个页面不可用。

---

# 六、地图桌面布局同时修复

目前：

```text
左侧医院列表很长
右侧地图高度有限
```

用户向下滚动以后：

```text
左边继续大量医院
右边变成大片空白
```

请改成真正的地图工作区。

推荐结构：

```text
┌─────────────────┬──────────────────────────┐
│ 医院列表         │                          │
│                 │                          │
│ 独立滚动         │        地图              │
│                 │                          │
│                 │        sticky / 固定      │
│                 │                          │
└─────────────────┴──────────────────────────┘
```

Desktop 下：

```text
地图和资源区高度与 viewport 有明确关系
左侧列表内部滚动
右侧地图保持可见
```

Mobile：

正常单列。

不要为了这个页面引入复杂布局框架。

---

# 七、问题 2：未验证的医院能力分仍然参与推荐

重点读取：

```text
data/regions/320400/hospitals/catalog.json

backend/app/domain/recommendation/features.py
backend/app/domain/recommendation/candidate.py
backend/app/domain/recommendation/scoring.py
backend/app/composition.py
```

当前医院目录已经正确区分：

```text
public facts
derived features
unsupported fields
```

这是对的。

但目前仍存在：

```json
"derived_capability_scores": {
  "心血管内科": 96,
  "骨科": 95
}
```

而 provenance 已明确：

```text
status = provisional
legacy catalog import
未完成外部核验
```

问题：

# 这些未核验分数仍然直接参与 clinical recommendation score。

这是不允许继续存在的。

---

# 八、医院推荐本轮原则

当前没有可靠来源支持的：

```text
96
98
91
```

等旧能力分：

# 不允许直接决定医院排序。

不要删除这些历史字段。

可以保留用于：

```text
migration
debug
future provenance work
```

但是正式推荐默认不能把它当可信能力指标。

---

# 九、医院 clinical score 暂时使用可解释事实

在正式 provenance 完成前，推荐应优先依赖：

```text
目标科室是否存在
相关科室是否存在
医院等级
医院类型
特殊人群匹配
急诊能力
位置 / 距离
已通过质量门的数据
```

例如可以采用明确、简单、可解释的规则：

```text
exact department exists
related department exists
no related department
```

不要编造新的：

```text
87
93
97
```

数字评分体系。

---

# 十、必须确保旧 provisional score 不偷偷进入其他路径

全面搜索：

```text
derived_capability_scores
strength_scores
strength_score
hospital_strength
```

检查：

```text
Hospital recommendation
Doctor recommendation
Risk penalty
Continuity
Resource tier
Explanation
```

是否仍依赖旧分数。

如果仍使用：

要明确判断是否合理。

---

# 十一、推荐解释也必须同步

如果能力分不再正式参与排序：

前端/后端不得继续说：

```text
“该医院某科能力 96 分”
```

或者暗示这是官方评分。

可以说：

```text
设有对应科室
公开资料显示存在相关专科方向
医院等级符合当前路径要求
```

---

# 十二、问题 3：医生推荐中的科研指标权重过高

重点检查：

```text
ENHANCED_WEIGHTS
academic_score
doctor_resource_tier
score_doctor_candidate
doctor_recommendation_reasons
```

当前代码可能使用：

```text
SCI 数量
总论文数量
国家级基金
专利
学术荣誉
```

计算：

```text
academic_score
```

然后用于医生推荐排序。

尤其某些场景中：

```text
academic weight
```

可能高达约：

```text
0.28
```

这个设计需要修正。

---

# 十三、核心原则

必须明确：

# 科研水平 ≠ 临床诊疗质量

SCI、基金、专利可以用于：

```text
医生公开资料
科研画像
学术背景
```

但不能作为：

> “这个医生更适合当前患者”

的核心证据。

---

# 十四、重新审计医生推荐特征

正式推荐核心特征优先：

```text
专科/亚专科匹配
医生公开擅长方向
医院/科室匹配
临床相关经历
复诊连续性
到院可达性
资源层级合理性
```

次级辅助：

```text
职称
公开履历
```

只适合展示/弱参考：

```text
SCI
论文
基金
专利
学术荣誉
```

---

# 十五、不要简单把 academic 全删掉

你需要先理解现有推荐策略。

然后设计：

```text
最小风险修改
```

可以：

```text
大幅降低 academic 权重
```

或者：

```text
从 patient-fit 主分移除
仅作为极弱 tie-break / explanation metadata
```

但必须说明理由。

---

# 十六、Doctor Resource Tier 也需要检查

如果：

```text
top_expert
expert
```

大量依赖：

```text
academic_score
```

则需要重新判断。

“专家资源层级”不应该等价于：

```text
论文多
+
基金多
```

---

# 十七、不要把学术资料删掉

医生详情页仍然可以展示：

```text
论文
科研项目
专利
学术任职
```

这些是真实公开画像的重要组成部分。

只是：

> 展示 ≠ 推荐依据。

---

# 十八、同时审计交通特征

这不是本轮主要修改目标，但必须检查。

当前后端仍然存在：

```text
bus
taxi
bike
traffic_access
```

而数据质量报告仍有大量异常。

检查：

```text
交通数据是否正在参与正式医院/医生排序。
```

如果是：

必须说明：

```text
哪些数据集参与
哪些字段参与
当前 quality status
```

如果当前数据属于：

```text
provisional
derived sample
quality gate failed
```

则不要默认把它作为强推荐依据。

如果只需很小修改即可增加 feature gate，可以完成。

如果涉及大范围业务变化：

不要扩大本次 scope。

记录到最终报告。

---

# 十九、模型评估不要在本轮重写，但必须审计

阅读：

```text
evaluation/model/evaluate_grouped.py
evaluation/model/grouped_split_report.json
evaluation/model/split_manifest.json
```

当前存在：

```text
Random Split
Exact Fingerprint Grouped Split
```

但数据中：

```text
Jaccard >= 0.8
```

近重复样本较多。

请判断：

> exact fingerprint group split 是否真的解决了近重复泄漏。

不要凭感觉回答。

阅读代码和报告后给出判断。

---

# 二十、如果你确认 exact group 不够严格

不要本轮直接重写整个模型。

在最终报告中给出下一步明确建议：

```text
Near-duplicate Group Split
```

例如：

```text
Jaccard >= threshold
→ connected components
→ component 不能跨 train/test
```

并说明：

```text
为什么
如何验证
可能造成的指标变化
```

---

# 二十一、前端 Copy Audit

同时检查 React 正式页面中是否还存在开发/Agent 语言。

例如：

```text
后续切片
比赛
评委
prompt
migration
demo task
TODO
暂未实现
```

不应该暴露在正常产品界面。

已知首页可能存在类似：

```text
语音输入将在后续切片接入
```

这种开发提示。

如果功能没做：

```text
直接隐藏/删除按钮
```

通常优于：

```text
放一个 disabled 未完成功能
```

不要假装已实现。

---

# 二十二、不要主动恢复所有旧功能

旧版本曾经包含：

```text
语音
浮动 Assistant
收藏
Dashboard
Demo Login
公交大屏
```

本轮不要恢复。

只记录：

```text
哪些能力还存在后端
哪些已删除
哪些值得未来恢复
```

---

# 二十三、当前应明确保留的新产品方向

产品应该继续保持：

```text
分诊辅助
→ 安全判断
→ 科室
→ 医院
→ 医生
→ 地图
→ 导航
```

而不是退回：

```text
后台管理系统
```

不要新增管理 Dashboard。

---

# 二十四、测试要求

修改后必须运行：

```bash
python -m pytest
python -m evaluation.safety.evaluate_safety

cd frontend
npm run typecheck
npm run test
npm run build
npm run e2e
```

如果仓库已有对应 `.venv` 或命令约定，以 README/CI 为准。

---

# 二十五、地图必须做真实浏览器检查

这一项非常重要。

不要只检查：

```text
HTTP 200
console 无 error
```

必须实际打开页面查看：

```text
/map
```

确认：

```text
没有 API KEY REQUIRED
没有 broken tiles
没有大面积空白
医院 marker 可见
点击列表可联动
点击 marker 可查看医院
高德导航链接可用
```

---

# 二十六、截图验收

至少检查：

```text
1440×900

390×844
```

页面：

```text
/
/triage
/resources
/map
/trust
```

重点关注：

```text
Map
Resources
Routine Result
Emergency
```

---

# 二十七、禁止为了测试绿而 mock 地图 UI

地图必须是真实网页运行结果。

不要：

```text
测试环境显示假地图
生产环境再请求 tile
```

来规避问题。

---

# 二十八、安全回归必须保持

当前 Safety baseline：

```text
38 cases
Red Flag Recall = 1.0
Under-triage = 0
Over-triage = 0
Emergency FN = 0
```

你的修改不应该让 Safety 回归退化。

这些只是工程固定样例，不代表临床验证。

不要修改文案为：

```text
100% 安全
```

---

# 二十九、修改范围控制

本轮理想文件范围大致：

```text
frontend/src/pages/MapPage.tsx
frontend/src/styles/globals.css

backend/app/domain/recommendation/features.py
backend/app/domain/recommendation/scoring.py
backend/app/domain/recommendation/candidate.py
backend/app/composition.py

相关 tests
docs/status/current.md
docs/risks/register.md
```

实际以代码结构为准。

如果开始修改二三十个不相关文件，先停下来重新判断 scope。

---

# 三十、完成以后做一次自我审计

回答：

```text
1. 地图真实显示了吗？
2. 地图 tile 是否需要 secret？
3. 未验证医院能力分还会影响排序吗？
4. SCI/基金/专利还会显著决定医生推荐吗？
5. 修改有没有改变 Safety 行为？
6. 有没有新增未经来源支持的数据？
7. 有没有开发提示词/Agent 文案出现在 UI？
8. 有没有恢复不该恢复的旧后台功能？
```

只要有一项答案不理想：

继续修复。

---

# 三十一、最终不要只说“完成”

请给我一份结构化报告：

```text
MiMo-X Audit Result

1. Baseline
- latest commit
- working tree
- tests before changes

2. Map
- root cause
- fix
- real browser verification
- fallback

3. Hospital Ranking
- previous problem
- fields removed from trusted ranking
- new clinical feature logic
- remaining limitations

4. Doctor Ranking
- previous academic influence
- new weights / policy
- why

5. Transit
- currently used?
- data trust level
- recommendation risk

6. Model Evaluation Audit
- random split
- exact grouped split
- near-duplicate risk
- recommended next step

7. Frontend Copy Audit
- removed internal/dev wording

8. Tests
- pytest
- Safety
- frontend test
- build
- Playwright

9. Modified files

10. Remaining P0 / P1
```

---

# 三十二、我评价你的标准

我不会因为你：

```text
改的文件多
代码写得长
界面更花哨
```

而认为你表现好。

我主要评价：

### 代码阅读

你是否真正理解现有系统，而不是机械搜索替换。

### 判断能力

你是否能区分：

```text
bug
数据可信度问题
产品设计问题
代码风格问题
```

### 修改克制

能否用尽量小的变化解决真正问题。

### 医疗安全意识

会不会为了功能完整牺牲安全边界。

### 数据意识

会不会把来源不明的数字继续包装成真实事实。

### 测试能力

会不会发现：

> 自动测试绿，但实际页面坏了

这种问题。

---

现在开始。

先审计，不要立刻大改。

确认根因后再实施最小修改。
