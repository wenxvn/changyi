# Review：医生 candidate 过滤边界

日期：2026-09-10

## 第一层：计划对齐

通过。查询词、科室关系和关键词召回 helper 已接入 legacy，正/反例、快照和记录均完成；未改变过滤条件、排序或 API 字段。

## 第二层：系统完整性

通过。candidate 模块只依赖显式医生、查询词和映射输入；不读取 Web、全局数据、模型或 repository。模型/分诊摘要仍由 `app.py` 生成后注入。

## 第三层：生产准备度

发现问题但不阻塞本切片：关键词来源、医生逐字段 provenance、医院 candidate 组装、交通组合、急症 explain 和 `fairness` 语义仍需审查；远端 CI 首次运行待推送后确认。

## 结论

本切片可进入医院 candidate/交通组合拆分；关键词命中只作为召回信号，不作为医学事实。
