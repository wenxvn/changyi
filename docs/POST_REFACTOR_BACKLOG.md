# Post-refactor backlog

本文件只登记任务，不代表已授权或已实现。每项都必须按 `AGENTS.md` 的 L3/L4 规则单独规划、评估和回滚。

## 旧能力盘点（2026-09-11 审计）

本轮不恢复旧功能，仅登记状态，便于后续决策。

| 能力 | 当前状态 | 说明 |
| --- | --- | --- |
| 语音输入 | 已从前端删除 | 此前仅有 disabled 按钮与“后续切片”文案；后端无对应 v1 API |
| 浮动 Assistant | 已删除 | 旧单体 UI 遗留，无 v1 契约 |
| 收藏 | 已删除 | 无账号体系，不恢复本地收藏以免被误解为病历 |
| 后台 Dashboard | 已删除 | 产品方向保持“分诊→医院→医生→地图→导航”，不做管理系统 |
| Demo Login | 已删除 | 演示认证易被误解为生产安全（R-007） |
| 公交大屏 | 已删除 | 旧独立展示页；当前仅保留 bus 站点样本用于弱可达性 |
| 医院/医生公开资料浏览 | **保留** | `/resources` + `/api/v1/hospitals|doctors` |
| 分诊 / Safety Gate / Follow-up | **保留** | 正式主路径 |
| 地图 + 高德导航 URI | **保留** | 外部跳转，非实时路线 |
| Trust Center / 评估证据 | **保留** | random + exact grouped split + 近重复说明 |
| 本地历史（F11 Profile） | **保留** | 默认关闭、脱敏、仅当前浏览器 |

值得未来评估恢复的（需另立 L3/L4）：语音输入（无障碍）、可解释收藏/关注列表（明确非病历）。其余旧后台能力不建议恢复。

## Safety / Medical

- 扩展专业复核后的 Safety Evaluation 覆盖，检查未纳入固定样例的红旗表达、否定/历史语境和信息不足边界。
- 继续审核压榨样胸痛伴呼吸困难/冷汗、反复头晕等自然语言组合；当前固定样例已回归通过，但不代表医学覆盖完整。
- 评审红旗规则、分诊阈值、医学文案和结构化 follow-up answer 契约。

## Data

- 完成医院/医生逐字段来源、许可证、更新时间、脱敏状态和正式发布范围。
- 处理数据质量报告登记的 187 个异常；不直接修改原始数据。
- 医院目录外置、结构化 Follow-up、位置正确性、模型 grouped split 和资源/地图闭环已在 P0 正确性计划中完成；后续只处理外部核验和专业评审，不重复实现同一切片。

## Product / Frontend

- 评估正式附近急诊路径、实时急诊可用性和更完整的视觉/对比度/无障碍矩阵；当前高德 URI 只提供外部导航跳转。
- 任何新的 Profile、账号、云同步或医疗档案能力必须先做隐私与认证 L4 评审。

## Infrastructure

- 如确有需求，另立生产认证、授权、审计、数据库、部署和监控方案；演示登录/CORS 不能直接升级为生产安全措施。
- 继续观察远端 CI 首次运行结果；只修当前基线相关问题。
