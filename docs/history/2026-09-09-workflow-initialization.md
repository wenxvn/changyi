# H-20260909-001：建立重构工作流基线

- 日期：2026-09-09
- 类型：治理初始化
- 状态：已完成
- 关联提交：`ba74fd0`（初始化仓库；本次工作流文件尚未提交）

## 背景

用户希望在重构前建立一套完整、可持续、可追溯的工作流，并要求读取项目和现有 skills 后补足必要规则。

## 观察

- 仓库没有根 README、测试套件、CI 或正式项目记录。
- 后端主要集中在 `app.py`，前端主要集中在 `static/js/app.js` 和 `static/css/style.css`。
- 项目已有 `architect`、`imprint`、`recover`、`remember`、`review` 五个协作 skill。
- 模型和项目文档明确声明当前系统只能用于数据建模、分诊辅助或学习演示。
- 当前工作区存在未跟踪的 `skills/`，本次已读取但没有修改其内容。

## 行动

- 新增根 README 和 `AGENTS.md`。
- 新增 `docs/` 记录体系、项目章程、当前架构、ADR、历史、进度、计划、风险、质量和模板。
- 建立首轮渐进式重构路线图，不在本次工作中改变业务代码。
- 创建本次会话的 `memory.md`，不记录任何凭证或敏感数据。

## 结果

工作流入口、记录命名、变更分级、医学安全边界、验证要求、故障恢复和会话交接规则已经具备，可作为后续重构的共同基线。

## 后续

先补充 API/纯函数行为基线和最小测试入口，再开始拆分数据加载边界。UI 首次建立一致性基线前，按 `imprint audit` 流程完成审计并等待确认。
