# H-20260911-001：前端键盘与交互语义收口

- 日期：2026-09-11
- 类型：前端无障碍 / L1 行为保持重构
- 结果：完成首版

## 事件

将并行前端剩余的隐式按钮和不完整 tab 语义收口：复用 `Button` 默认不提交表单；首页 AI Journey、资源筛选和地图筛选都声明 tab/panel 关系；Journey 增加方向键、Home 和 End 导航；移除已被实际页面替代的占位页。

## 安全与兼容

- 没有增加请求、持久化、医学判断、分诊状态或推荐策略。
- 地图仍只展示资源位置和“含急诊字段”标记，不生成实时急诊或导航结论。
- legacy 默认入口、`/api/*`、`/api/v1/*` 和新页面数据契约不变。

## 证据

前端 typecheck、9/9 boundary tests 和 production build 通过；完整辅助技术矩阵仍待后续 E2E/视觉门禁。
