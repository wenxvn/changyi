# H-20260910-006：完成并行前端医疗资源浏览首版

- 类型：前端资源索引、来源提示、记录
- 变更等级：L2
- 结果：F7/F8 资源浏览首版完成；legacy 默认入口保持不变。

## 事件

将 `/resources` 从占位页推进为只读资源索引。医院索引首屏加载，医生资料在用户切换 tab 后按需加载；医院/医生卡片支持关键词筛选和公开资料预览。

## 安全与兼容

- 资源页不复制分诊、疾病识别、推荐排序或急症判断。
- 医院目录继续展示 `legacy_catalog_pending_provenance` 状态；医生显示 `public_source_mixed`，不包装成官方推荐。
- 地图入口明确为建设中，未用普通资源列表代替附近急诊路径。
- 未修改 legacy `/`、旧 API、Safety Gate、推荐权重或模型。

## 验证

前端 typecheck、4 个 boundary tests、Vite build、86 个 pytest，以及医院/医生索引、搜索、资料预览、桌面/390×844 运行态检查通过；医生首次请求依赖清理 bug 已在浏览器验证中发现并修复。

## 后续

补真实资源详情契约、F9 地图/急诊路径、F10 Trust Center，再完成 Playwright、视觉、移动和无障碍门禁。
