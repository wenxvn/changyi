# AGENTS.md — 常医重构协作规则

本文件是仓库级协作规则，适用于整个项目及其子目录。任何自动化 agent、开发者或审查者开始工作前都必须阅读本文件；子目录可以增加更严格的 `AGENTS.md`，但不能削弱本文件中的安全、质量和记录要求。

## 1. 项目使命与边界

本项目是常州市医疗资源推荐、分诊辅助和数据可视化的演示型系统。它可以帮助用户整理症状、理解医院和医生资源、展示推荐依据，但不能声称完成医学诊断、急救指令、处方或临床决策。

以下内容始终在范围内：

- 医院、医生、科室和交通数据的展示、查询与可解释推荐。
- 症状标准化、分诊辅助、追问建议和模型实验。
- 在不改变既有外部行为的前提下拆分后端、前端、数据加载和模型边界。
- 建立可验证、可回滚、可追溯的工程流程。

以下内容默认不在范围内，除非用户明确授权并另行完成安全与合规评审：

- 把演示系统包装成临床诊断或医疗器械产品。
- 引入真实患者身份信息、病历、联系方式、凭证或未脱敏日志。
- 删除数据来源、许可证、免责声明、低置信度提示或人工复核入口。
- 以“重构”为名顺便改变推荐策略、医学规则、数据口径或 UI 交互。

## 2. 读取顺序与事实来源

每次工作开始按以下顺序读取：

1. `AGENTS.md`。
2. `docs/README.md` 与 `docs/status/current.md`。
3. 当前任务关联的 `docs/plans/`、`docs/decisions/`、`docs/risks/` 和 `docs/reviews/`。
4. 关联目录下的更具体规则文件和 `skills/*/SKILL.md`。
5. 最后读取要修改的代码、模板、数据和测试。

事实来源优先级如下：

1. 当前用户明确提出的目标与约束。
2. 本文件及更具体目录级 `AGENTS.md`。
3. 已接受的决策记录（ADR）。
4. 已确认的计划、质量门禁和风险登记。
5. 当前代码和数据行为。
6. 未记录的口头推测、临时脚本输出和生成物。

如果来源相互冲突，不要静默选择；暂停实现，在 `docs/decisions/` 记录冲突、影响和待确认项。

## 3. 标准工作流

### 3.1 会话开始

- 运行 `git status --short --branch`，识别用户已有改动；不得覆盖、重置或清理不属于当前任务的改动。
- 读取 `memory.md`（如存在）、当前进度、进行中的计划和开放风险。
- 明确本次任务的目标、非目标、验收标准、预计影响范围和回滚方式。
- 如果是新功能或架构变化，先使用 `skills/architect/SKILL.md` 的思路对齐术语和关键决策。
- 如果只是计划设计，使用 `create-plan` skill 的结构；如果用户要求落地，则以该计划为实施依据，不因“计划模式”而停止写入。

### 3.2 变更分类与审批门槛

先给变更标记一个等级：

- `L0`：文档、记录、注释或格式变化，不改变运行行为。
- `L1`：纯 UI 样式、文案或非业务交互变化。
- `L2`：数据加载、API、前端状态或非医学排序逻辑变化。
- `L3`：分诊、症状标准化、疾病模型、推荐排序、风险提示或数据口径变化。
- `L4`：认证、安全、隐私、生产部署、真实数据接入或不可逆迁移。

`L0`–`L1` 至少需要计划和验证记录；`L2` 需要 API/行为基线和回归验证；`L3` 必须有明确的医学安全说明、样例/反例、低置信度策略和人工复核策略；`L4` 必须有专门的风险评审和用户确认。不能把高等级变化伪装成重构。

### 3.3 实施

- 先写或更新 `docs/plans/YYYY-MM-DD-<slug>.md`，把工作拆成可回滚的小步骤。
- 大型重构采用“表征测试/行为快照 → 单一边界拆分 → 回归比较 → 删除旧路径”的渐进方式，不做一次性大爆炸重写。
- 一次提交只解决一个清晰主题；提交信息使用 `docs:`, `refactor:`, `feat:`, `fix:`, `test:`, `chore:` 等前缀。
- 保持现有 API 响应字段、状态码、URL 和关键 UI 流程，除非变更计划明确说明迁移策略。
- 不在路由函数中新增复杂业务逻辑；优先将纯逻辑、数据访问、模型调用、排序策略和展示层分开。
- 数据文件不得手工改写而没有来源、时间、许可证和校验说明；生成数据与原始数据分开存放。
- 不把密钥、口令、Cookie、访问令牌、连接串、真实患者信息或敏感日志写入代码、文档、测试快照或 `memory.md`。
- 不为了“保持整洁”使用 `git reset --hard`、`git checkout --`、递归删除或覆盖用户文件。

### 3.4 验证与复核

每次实施必须根据等级运行验证，并记录“运行了什么、结果是什么、没验证什么”：

- Python：`python3 -m py_compile` 覆盖修改后的模块；关键 API 使用 Flask test client 做烟雾测试。
- JavaScript：至少运行 `node --check static/js/app.js`；有 UI 变化时进行浏览器手工验证。
- API：验证成功、空输入、非法输入、缺失数据、模型不可用和 404 等边界。
- 医学逻辑：检查急症/红旗症状、低置信度、症状不足、已知疾病和模型不可用路径。
- 数据：检查 JSON/CSV 可解析、字段口径、数量变化、来源和脱敏状态。
- UI：UI 变更后按 `skills/imprint/SKILL.md` 更新一致性记录；现有 UI 第一次建立基线前先运行 audit 并等待基线确认。
- 完成前使用 `skills/review/SKILL.md` 的三层复核：计划对齐、系统完整性、生产准备度。

验证失败时，先按 `skills/recover/SKILL.md` 判断是局部 bug、污染会话还是基础假设错误，再决定定点修复、重置会话或重新设计。不得连续叠加未经诊断的补丁。

### 3.5 会话结束

- 更新 `docs/progress/` 的当前进度和下一步。
- 有架构、数据口径、安全或医学策略变化时新增或更新 ADR。
- 把重要事件写入 `docs/history/`；失败尝试和复盘也要记录，但不要写入秘密。
- 运行 `skills/remember` 的保存规则，更新根目录 `memory.md`，只保存下一次会话真正需要的非敏感上下文。
- 检查 `git diff`、`git status`、测试结果和未记录的生成物，再向用户汇报。

## 4. 目录职责

| 路径 | 用途 | 规则 |
| --- | --- | --- |
| `app.py` | 当前 Flask 单体入口和兼容层 | 重构初期保持路由兼容，不继续堆积复杂逻辑 |
| `data/` | 结构化数据、模型、训练报告 | 必须有来源、口径和脱敏说明 |
| `templates/` | HTML 模板 | 只负责页面骨架，避免加入业务算法 |
| `static/js/` | 浏览器行为和展示协调 | API 调用、状态、渲染逐步模块化 |
| `static/css/` | UI 样式 | 统一使用 CSS token，遵守 UI registry 流程 |
| `scripts/` | 数据处理、爬取和构建脚本 | 明确输入、输出、幂等性和外部依赖 |
| `docs/` | 计划、架构、决策、风险、质量和历史记录 | 记录事实、决定和可复现步骤，不复制整段代码 |
| `skills/` | 项目协作 skills | 修改 skill 前必须说明影响范围并更新 workflow 文档 |
| `ppt_build_tmp/` | 本地生成物 | 默认不提交，不作为系统事实来源 |

## 5. 医学、数据和安全规则

- 所有模型输出都必须标记为辅助信息，不得以确定语气替代诊断。
- 红旗症状必须优先于普通推荐；不能被距离、价格、排序偏好或 UI 视觉弱化。
- 模型置信度低、输入不足、数据缺失或模型不可用时，必须明确降级并建议人工/专业复核。
- 推荐解释必须区分“用户输入事实”“数据集事实”“系统推断”；不能把推断包装成医院或医生的官方承诺。
- 医生、医院和图片数据需要保留来源、抓取/更新时间、许可状态和脱敏状态；外部网页内容未经审核不得直接当作医疗事实。
- 日志只记录排查所需的最小信息；测试反馈不得包含真实身份信息或病历原文。
- 演示登录、默认密码和本地 CORS 不能被误认为生产安全措施；任何生产化工作必须拆成 `L4` 任务。

## 6. 何时使用项目 skills

- `architect`：新功能、架构拆分、数据契约或会改变多个模块的重构开始前。
- `remember`：每次会话开始恢复、结束保存；不得保存任何秘密。
- `review`：每个功能或重构切片完成后，只报告问题，不擅自掩盖问题。
- `recover`：出现失败、反复修复或方向不确定时，先诊断失败类型。
- `imprint`：新增或修改 UI 组件后，维护 `ui-registry.md`；现有 UI 首次建立基线时先 audit。

## 7. 完成定义

只有同时满足以下条件，任务才可标记为完成：

1. 目标和非目标与计划一致。
2. 代码、数据、文档和风险记录已同步。
3. 相关自动化/手工验证已运行并记录结果。
4. 未引入秘密、未覆盖用户改动、未留下未说明的生成物。
5. 医学安全、错误处理、空态和降级路径经过检查。
6. 下一步、剩余风险和回滚方式对下一次会话清晰可见。
